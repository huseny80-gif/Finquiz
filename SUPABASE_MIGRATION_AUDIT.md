# تدقيق ما قبل الترحيل — PHASE A: INSPECT + AUDIT

**تاريخ التدقيق:** 2026-09-11
**النطاق:** فحص فقط — لا تعديل على أي كود في هذه المرحلة.

## ملاحظة أولى وحاسمة قبل أي شيء آخر

الطلب الذي وصلني يفترض أن المشروع لا يزال **Static Content Architecture خالصة** يحتاج ترحيلاً من الصفر
نحو Supabase. **هذا الافتراض غير صحيح لحالة المستودع الآن.** جزء كبير جداً من هذا الترحيل **منفَّذ
ومُدمَج فعلياً في `main`** عبر عمل سابق في هذه الجلسة نفسها (PR #20 وPR #21، كلاهما مدموج). إن تجاهلتُ
هذه الحقيقة وبدأت "من الصفر" وفق حرفية الطلب، فسأُعيد بناء أشياء موجودة، وأُدخل تعارضات حقيقية بين
تصميم مقترح جديد وتصميم قائم فعلاً على القاعدة الحية (منشورة، بها بيانات، ومحكومة بسياسات RLS تعمل
الآن). لذلك هذا التدقيق يوثّق **الواقع الفعلي أولاً**، ثم يقيس فجوة الطلب الجديد (لوحة إدارة كاملة
CRUD + نظام رفع ملفات + Storage + أدوات هجرة/استيراد + توثيق موسّع) على هذا الواقع — لا على افتراض
خاطئ ببداية من نقطة الصفر.

---

## 1. الوضع الحالي

### 1.1 مصدر البيانات وHydrate

التطبيق لا ينتظر الشبكة أبداً قبل أول رسم. `assets/js/app.js` (`init()`, أسطر 154–198) يرسم الهيكل
فوراً من `DLP.data` الحالية (المصدر الثابت)، ثم يستدعي `DLP.store.hydrate()` في الخلفية.

- **المسار الثابت (Demo/Static — المصدر الافتراضي عند فشل أي شيء)**: `index.html` (أسطر 61–66) يحمّل
  `data/subjects/index.js` + خمسة ملفات مواد (`ai-data.js`, `legal-regulatory.js`,
  `cybersecurity-governance.js`, `innovation-project-management.js`, `risk-management.js`) عبر
  `<script>` عادية (لا نظام وحدات). كل ملف يكتب مباشرة في `global.DLP.data[subjectId]`.
- **مسار القاعدة (فعّال حالياً، ليس مخططاً مستقبلياً)**: `data/config/supabase.js` يحمل
  `enabled: true` **بالفعل**. `assets/js/core/supabase.js` يبني عميل Supabase من حزمة محلية
  (`assets/vendor/supabase-js.min.js`، محمَّلة محلياً بسبب CSP الحالية `script-src 'self'` — **لا
  تحميل من CDN**) فقط إذا توفّرت `url/publishableKey`؛ أي فشل يترك `DLP.supabaseClient = null` بلا
  أي استثناء.
- **`hydrate()`** (`core/store.js`): مهلة داخلية **6 ثوانٍ** (`HYDRATE_TIMEOUT_MS`)؛ عند النجاح
  يستبدل `DLP.data`/`DLP.subjectOrder` ذرّياً ويُحوّل `dataSource()` من `'static'` إلى `'database'`؛
  عند أي فشل/بطء يسقط بهدوء على الثابت المعروض فعلاً.
- **`DLP.store.dataSource()`** هي الطريقة الوحيدة المعتمَدة لقراءة مصدر البيانات، وتُستخدَم في
  `quiz-view.js` (`remoteMode()`) للتفرّع بين التصحيح المحلي والتصحيح عبر RPC.
- شكل `DLP.data` **متطابق تماماً** بين المصدرين (هذا هو العقد الأساسي لكل الطبقة): كل مادة
  `{id, order, title, shortTitle, icon, accent, status, description, lectures[], summaries[],
  assignments[], quizzes[], references[], resources[], updates[]}`.
- بعد نجاح `hydrate()` متأخراً، `app.js` يستدعي `DLP.quizView.refreshAllQuestionObjects()` لتحديث
  كائنات الأسئلة **بلا** إعادة رسم قسرية للصفحة الحالية (قرار موثَّق صراحة في الكود لتفادي سباق
  حقيقي اكتُشف عبر CI — انظر `IMPLEMENTATION_REPORT.md`).

`assets/js/core/api.js` هو **الطبقة الوحيدة المخوَّلة** بالتحدث مباشرة إلى Supabase — موثَّق صراحة في
رأس الملف. هذا يعني: **الطبقة المطلوبة في المرحلة 4 من الطلب (Service/Repository Layer) موجودة فعلاً
بمسمّى مختلف (`core/api.js` بدل `assets/js/services/*.js`)**، وليست فجوة معمارية، بل قرار تسمية سابق.

### 1.2 DLP.store

يُصدِّر: `SECTIONS` (ترتيب الأقسام السبعة)، `isVisible()`, `list()`, `subjects()`, `getSubject()`,
`subjectCounts()`, `countQuestions()`, `stats()`, `latestUpdates()`, `findQuiz()`, `dataSource()`,
`hydrate()`. يُستهلَك من `app.js`, `components/subject.js`, `components/quiz-view.js`,
`components/home.js`, `components/search.js`, `components/dashboard.js`, `components/admin/index.js`.

### 1.3 الموجّه (Router)

`assets/js/core/router.js` — موجّه Hash بسيط: `add/parse/resolve/handle/navigate/start`. المسارات
المسجَّلة فعلياً في `app.js` (11 مساراً، من بينها **`#/admin` و`#/admin/quiz/:quizId` موجودان فعلاً
اليوم**، بلا رابط تنقّل ظاهر عمداً). دعم `?focus=` للتنقّل الداخلي موجود ويُستخدَم فعلياً.

### 1.4 عرض المواد/المحاضرات

`assets/js/components/subject.js` يرسم 7 تبويبات (`lectures, summaries, assignments, quizzes
(→ DLP.quizView), references, resources, updates`) بدعم ARIA/roving-tabindex كامل. المحاضرات لا تعرض
"محتوى" هيكلي حقيقي بعد — فقط وصف + أهداف + ملفات + شارة "المحتوى قادم قريباً" (`lecture.contentSoon`).

### 1.5 عرض الملفات — **فجوة حقيقية مكتشَفة**

الملفات في البيانات الثابتة كائن مضمَّن `{type, label, url}` — لا يوجد `storage_path` إطلاقاً في
`data/subjects/*.js`. **لكن قاعدة البيانات الحية بها بالفعل جدول `files` عديم الشكل (polymorphic) بأعمدة
`storage_path`/`public_url`** (انظر §9) — **غير مُستخدَم فعلياً**: `api.js#mapLecture/mapSummary/
mapAssignment` يُثبّت `files: []` دائماً. أي أن **وضع القراءة الحية اليوم يعرض قوائم ملفات فارغة لكل
محاضرة/ملخص/واجب**، بينما الوضع الثابت يعرض ملفات حقيقية — **انحراف تكافؤ (parity drift) قائم فعلاً بين
المصدرين**، مستقل تماماً عن أي طلب جديد، ويجب إصلاحه بغضّ النظر عن نطاق هذا الطلب.

### 1.6 حفظ تقدّم الاختبارات

`localStorage` (`dlp.quiz.<quizId>`) لا يزال المصدر الأساسي في الوضع الثابت — **لم يُمَس**. في وضع
القاعدة، مسار تصحيح خادمي **كامل ومُطبَّق فعلاً** عبر RPCs: `start_quiz_attempt`, `save_quiz_answer`,
`finish_quiz_attempt`, `reveal_question_answer`, `check_answer` (الأخيرة للزوار غير المسجَّلين، تصحيح
فوري بلا حفظ ولا كشف بنك الإجابات). `core/quiz.js` (التصحيح المحلي الثابت) **لم يُمَس ولن يُمَس** —
يُستدعى فقط حين `!remoteMode()`.

### 1.7 الاختبارات الحالية

- `tests/run.js`: ~113–119 اختبار وحدة (يشمل مجموعات Supabase: طبقة الاتصال، الحفظ الدائم، التصحيح
  الخادمي، اللوحة الطلابية، سقالة الإدارة القراءة-فقط).
- `tests/e2e.js`: ~56–65 اختبار متصفح (Playwright)، يعمل غالباً على المسار الثابت في بيئات الشبكة
  المحجوبة.
- `tests/e2e.live.js`: **اختبار حي حقيقي متصل فعلياً بمشروع Supabase الحي**، محروس بسرَّي
  `E2E_LIVE_EMAIL`/`E2E_LIVE_PASSWORD`، يتخطّى نفسه بهدوء بغيابهما، مُدمَج في وظيفة CI منفصلة (`live`).
- `package.json`: `test`, `test:e2e`, `test:e2e:live`, `validate`, `build` (= validate+test فقط، بلا
  e2e). `.github/workflows/ci.yml` بها 3 وظائف فعلية: `verify`, `browser`, `live`.

### 1.8 علاقات البيانات وتسمية المعرّفات

نمط ثابت: `<بادئة المادة>-<نوع><رقم>[-<تسلسل>]` — مثال `rm-q1-13` (مادة risk-management، quiz1، سؤال
13). `api.js#questionSeq()` يعتمد على هذا النمط صراحة (تحقّق فعلي: 164/164 معرّف سؤال حي يطابقه) لإعادة
بناء ترتيب المؤلف الأصلي بعد الجلب من القاعدة — **أي تغيير في نمط المعرّفات يكسر هذه الدالة**.

### 1.9 أسماء/حقول محتملة التعارض — **الأهم في هذا التدقيق**

انظر القسم المخصَّص "الفجوات والتعارضات" أدناه — هذا هو صلب ما طلبه المستخدم في البند 9، وتفصيله حرج
كفاية ليُفرَد له قسم كامل بدل فقرة هنا.

### 1.10 كود يعتمد مباشرة على `data/*.js`

لا يوجد نظام وحدات (لا `require`/`import`) — الاعتماد الحقيقي الوحيد هو **البنيوي/الترتيبي**:
`index.html` يحمّل 5 ملفات `<script>` بأسماء مطلوبة صراحة؛ إضافة مادة جديدة تتطلب تعديل 3 أماكن يدوياً
(`index.html`, `data/subjects/index.js`, ملف المادة الجديد) — **نقطة احتكاك حقيقية بحد ذاتها**.
المكوّنات الأكثر حساسية لشكل البيانات: `quiz-view.js` (يتفرّع صراحة بين شكلي match/order الثابت
والقادم من القاعدة)، `admin/questions.js` (يفترض شكل استجابة `admin_get_quiz_questions` RPC تحديداً)،
`scripts/generate-seed.js` (يولّد SQL من `data/subjects/*.js` — أي حقل جديد في البيانات الثابتة لن
يظهر في البذرة تلقائياً بدون تحديث هذا المولِّد).

---

## 2. الملفات المتأثرة (بحسب النطاق الفعلي المتبقّي، لا الطلب الحرفي الكامل)

### موجودة فعلاً ولن تُعاد كتابتها من الصفر
`data/config/supabase.js`, `assets/js/core/supabase.js`, `assets/js/core/api.js`,
`assets/js/core/auth.js`, `assets/js/core/store.js`, `assets/js/components/quiz-view.js`,
`assets/js/components/dashboard.js`, `assets/js/components/admin/index.js`,
`assets/js/components/admin/questions.js`, `supabase/migrations/001..009_*.sql`,
`supabase/seed/course2.sql`, `scripts/generate-seed.js`, `DATABASE_SCHEMA.md`,
`IMPLEMENTATION_REPORT.md`, `MIGRATION_PLAN.md`, `tests/run.js`, `tests/e2e.js`, `tests/e2e.live.js`,
`.github/workflows/ci.yml`.

### ستحتاج تعديلاً حقيقياً لتغطية الطلب الجديد (CRUD إداري + ملفات + Storage)
- `assets/js/core/api.js` — إضافة دوال CRUD (اليوم قراءة فقط بالكامل تجاه القاعدة).
- `assets/js/components/admin/*.js` — تحويل من قراءة-فقط إلى نماذج إنشاء/تعديل/حذف فعلية.
- `assets/js/components/subject.js` (`fileChip`) — وصل فعلي لجدول `files` (اليوم يعرض دائماً `[]` من
  القاعدة).
- `supabase/migrations/` — migration جديدة إن قرّرنا تعديل جدول `files` (تقسيمه أو إبقاءه polymorphic
  — قرار مطلوب، انظر أدناه) + سياسات RLS لعمليات الكتابة الإدارية (اليوم `content_write_admin` موجودة
  جزئياً لكن غير مُستدعاة فعلياً من أي مسار كتابة في الواجهة).
- `assets/js/core/auth.js` — إضافة بريد/كلمة مرور (اليوم Google OAuth فقط) إن أردنا فعلاً واجهة دخول
  بريد/كلمة مرور للمستخدمين العاديين (لا فقط اختبار CI).

### ملفات جديدة مطلوبة فعلياً لتغطية الطلب (غير موجودة اليوم)
`assets/js/services/storage-service.js` (رفع/تنظيف أسماء/التحقق من النوع والحجم)،
`scripts/import-files.js`، `IMPORT_REVIEW.md`، `CONTENT_ADMIN_GUIDE.md`، `SUPABASE_SETUP.md`،
`SUPABASE_SECURITY.md` (توثيق منفصل — اليوم موزَّع بين `DATABASE_SCHEMA.md`/`IMPLEMENTATION_REPORT.md`)،
`CONFIGURATION.md`، `MIGRATION_GUIDE.md`، نماذج إدارة فعلية لكل كيان (محاضرة/ملخص/واجب/اختبار/سؤال).

---

## 3. مخطط البيانات الحالي (الحي فعلاً على Supabase)

جداول موجودة ومطبَّقة: `roles`, `profiles`, `user_roles`, `courses`, `subjects`, `lectures`,
`summaries`, `assignments`, `quizzes`, `questions`, `question_options`, `question_pairs`,
`question_items`, `"references"` (اسم محجوز — مُقتبَس دائماً)، `resources`, `updates`, `files`
(عديم الشكل/polymorphic)، `quiz_attempts`, `quiz_answers`, `student_progress`.

نقاط تصميم حاسمة موجودة فعلاً:
- المفاتيح الأساسية لجداول المحتوى `text` مطابقة تماماً لمعرّفات البيانات الثابتة (`rm-l1` مثلاً) —
  **لا ترجمة معرّفات**. جداول المستخدمين (`profiles`, `quiz_attempts`, ...) تستخدم `uuid` مرتبطة بـ
  `auth.users`.
- **`options`/`pairs`/`items` ليست JSONB داخل `questions`** — بل ثلاثة جداول فرعية منفصلة
  (`question_options`, `question_pairs`, `question_items`) — قرار أمني متعمَّد (انظر §4).
- إخفاء الإجابات يتم على **مستوى العمود** (`GRANT`/`REVOKE SELECT` لأعمدة محدَّدة)، لا فقط على مستوى
  الصف (RLS) — لأن JSONB واحد لا يمكن إخفاء بعض مفاتيحه عبر GRANT، بخلاف عمود/جدول منفصل.
- `files` جدول واحد عديم الشكل بأعمدة `storage_path`/`public_url` اختياريتين (وليس إلزاميتين معاً) —
  يطابق فعلاً متطلب الطلب "لا تجعل الاثنين إلزاميين في الوقت نفسه"، لكن **غير موصول من الواجهة بعد**.
- `courses` موجود (`course-2` صف واحد)، و`subjects.course_id` يشير إليه — غير مذكور في مخطط الطلب
  الجديد إطلاقاً.

---

## 4. الفجوات والتعارضات (الأهم — يحتاج قراراً صريحاً منك قبل Phase B)

### 4.1 تعارض بنيوي: `lecture_files`/`summary_files`/`assignment_files` المقترحة مقابل `files` الموجود فعلاً

الطلب يقترح ثلاثة جداول ملفات منفصلة بحسب النوع. **الموجود فعلاً جدول واحد `files`** بأربعة أعمدة FK
اختيارية (`subject_id`, `lecture_id`, `summary_id`, `assignment_id`) — عديم الشكل بدل مقسَّم. تقسيمه
الآن يعني: migration تُنشئ 3 جداول جديدة + هجرة 45 صفاً مبذورة فعلاً + إعادة كتابة سياسات RLS المطبَّقة
على `files` + تحديث `scripts/generate-seed.js`. **هذا ليس إضافة، بل إعادة هيكلة**.

**قراري المقترح:** الإبقاء على `files` عديم الشكل كما هو (يحقّق نفس الهدف الوظيفي، ومُختبَر ومُطبَّق
فعلاً)، وربطه بالواجهة (وصل `mapLecture`/`mapSummary`/`mapAssignment` بجدول `files` الفعلي) بدل تقسيمه
إلى ثلاثة جداول. توفير هذا القرار عليك للموافقة الصريحة قبل أي تنفيذ.

### 4.2 تعارض بنيوي: `options`/`pairs`/`items` كأعمدة JSONB مقترحة مقابل جداول فرعية موجودة فعلاً

الطلب يقترح أعمدة `options jsonb`, `pairs jsonb`, `items jsonb`, `answer` مباشرة على `questions`.
**الموجود فعلاً** هو تقسيمها لثلاثة جداول فرعية (`question_options`, `question_pairs`,
`question_items`) **تحديداً** لأن حجب الإجابة الصحيحة (`is_correct`, `right_text`, ترتيب `position`)
على مستوى عمود واحد داخل JSONB **غير ممكن تقنياً** عبر `GRANT`/`REVOKE` في PostgreSQL — بينما هو ممكن
تماماً حين تكون هذه الحقول أعمدة منفصلة في جدول منفصل. الرجوع للتصميم المقترح (JSONB مضمَّن) **يفتح
ثغرة أمنية حقيقية** (تسريب الإجابة الصحيحة قبل التصحيح) كانت الهجرات 002/004/006/007/008 قد أُنشئت
تحديداً لإغلاقها.

**قراري المقترح:** رفض التصميم المقترح لهذا الجزء تحديداً، والإبقاء على التصميم الحالي المُطبَّق
(جداول فرعية + Grant/Revoke على مستوى العمود). هذا ليس تفضيلاً أسلوبياً، بل **متطلب أمني مثبَت**.

### 4.3 فجوة وظيفية مستقلة عن أي طلب جديد: عرض الملفات فارغ دائماً في وضع القاعدة

مذكورة في §1.5 — يجب إصلاحها بغضّ النظر عن قرار §4.1، لأنها انحراف تكافؤ فعلي قائم بين المصدرين اليوم.

### 4.4 `#/admin` قراءة-فقط اليوم — الطلب يريد CRUD كاملاً

هذا هو الجزء الأضخم فعلياً من الطلب (المراحل 8–15). لا يوجد تعارض تصميمي هنا — فقط حجم عمل حقيقي كبير:
نماذج إنشاء/تعديل/حذف لكل من المواد/المحاضرات/الملخصات/الواجبات/الاختبارات/الأسئلة، + رفع ملفات فعلي
إلى Supabase Storage (لا يوجد Bucket منشأ بعد إطلاقاً — لا `course-files` ولا غيره)، + سياسات RLS
للكتابة (موجودة جزئياً كـ`content_write_admin` لكن لم تُختبَر عبر أي مسار كتابة فعلي من الواجهة بعد).

### 4.5 بريد/كلمة مرور للمستخدمين العاديين غير موجود

اليوم Google OAuth فقط في `auth.js`. `tests/e2e.live.js` يستخدم `signInWithPassword` مباشرة كأداة
اختبار CI فقط، وليس ميزة مستخدم. إضافته كميزة مستخدم فعلية (المرحلة 7 من الطلب) قرار منتج يحتاج تأكيدك
(هل نريد فعلاً واجهة دخول بريد/كلمة مرور بجانب Google؟ أم يكفي إبقاء Google فقط؟).

### 4.6 `courses` غير مذكور في الطلب الجديد

قرار: الإبقاء عليه (صف واحد، مرجع بسيط) دون تعديل، لأنه لا يتعارض مع أي شيء ولا يستحق إزالته.

---

## 5. خطة الترحيل المقترحة (معدَّلة عن الطلب الحرفي لتوافق الواقع)

بما أن الأساس (Schema + RLS + RPCs + Demo fallback + قراءة حية + اختبار حي CI) **مُنجَز فعلاً**، تُعاد
صياغة مراحل الطلب (8–21 تحديداً) كخطة عمل فعلية بالترتيب:

1. **PHASE B — Storage + إصلاح فجوة الملفات**: إنشاء Bucket `course-files` بالهيكل المطلوب، وصل
   `api.js` بجدول `files` الفعلي (إصلاح §4.3)، سياسات Storage (قراءة عامة للمنشور، كتابة admin فقط).
2. **PHASE C — توسعة Service Layer للكتابة**: إضافة دوال CRUD في `core/api.js` (إنشاء/تعديل/حذف/تغيير
   حالة) لكل كيان، مع RPCs جديدة أو REST مباشر محكوم بـRLS كتابة (حسب الأنسب لكل حالة).
3. **PHASE D — لوحة الإدارة الفعلية**: تحويل `admin/*.js` من قراءة-فقط إلى نماذج فعلية (مادة → محاضرة
   → ملخص → واجب → اختبار/سؤال → ملفات → تحديثات)، بالترتيب المذكور في الطلب.
4. **PHASE E — بريد/كلمة مرور (إن أكَّدتَ الحاجة إليه) + توثيق الاستيراد** (`scripts/import-files.js`,
   `IMPORT_REVIEW.md`).
5. **PHASE F — التوثيق الموسّع**: فصل `SUPABASE_SETUP.md`, `SUPABASE_SECURITY.md`,
   `CONTENT_ADMIN_GUIDE.md`, `MIGRATION_GUIDE.md`, `CONFIGURATION.md` عن الوثائق الحالية المدمجة.
6. **PHASE G — اختبارات الأمان الإضافية** (XSS/RLS/رفع ملفات خبيثة) + تحديث CI إن لزم.

---

## 6. المخاطر

- **الخطر الأهم: تعارض تصميمي مباشر بين طلبك والتصميم الأمني القائم فعلياً** (§4.1, §4.2) — تنفيذ
  الطلب حرفياً يُضعف أماناً مُثبَتاً بالفعل. يجب حسم هذا صراحة قبل أي كتابة كود.
- **بيانات حية حقيقية على القاعدة** — أي migration جديدة تلمس `questions`/`files`/`quizzes` تعمل على
  بيانات مبذورة فعلياً (164 سؤال، 45 ملفاً...) لا على قاعدة فارغة؛ يجب أن تكون كل migration قابلة
  للتشغيل الآمن (idempotent) وتُختبَر بـ`execute_sql` قبل الاعتماد عليها، تماماً كما جرى سابقاً.
  **ملاحظة مهمة: هذه الجلسة لا تملك اتصال شبكة مباشراً بـ`supabase.co`** (سياسة egress تنظيمية خارج
  نطاق المشروع، مؤكَّدة سابقاً بـ`403 connect_rejected`) — أي تطبيق فعلي لـmigrations يعتمد على أدوات
  Supabase MCP (التي تصل عبر خادمها الخاص، لا عبر شبكة هذه الجلسة)؛ إن كانت هذه الأدوات غير متاحة وقت
  التنفيذ الفعلي، سأُوقف وأُبلغك بدل الافتراض.
- **حجم العمل الفعلي ضخم** (لوحة إدارة CRUD كاملة + Storage + رفع ملفات + أدوات هجرة) — تنفيذه دفعة
  واحدة (كما يحذّر الطلب نفسه من فعله) خطر حقيقي على الاستقرار؛ الالتزام بمراحل B→G تباعاً مع اختبار
  فعلي بعد كل مرحلة إلزامي.
- **Storage Bucket جديد كلياً** — لا Bucket موجود اليوم؛ إنشاؤه وسياساته يحتاج اختباراً فعلياً (رفع
  حقيقي، رابط عام، رفض وصول غير مصرَّح) قبل اعتماده، لا افتراض نجاحه من القراءة فقط.
- **بريد/كلمة مرور كميزة مستخدم** (إن اعتُمد) يفتح سطح هجوم جديد (تعداد بريد، هجمات كلمة مرور) يحتاج
  تقييماً أمنياً منفصلاً (Rate limiting على مستوى Supabase Auth، لا شيء إضافي مطلوب من الواجهة عادة،
  لكن يستحق ذكراً صريحاً في `SUPABASE_SECURITY.md`).

---

## 7. القرارات المقترحة (بانتظار موافقتك الصريحة على كل بند قبل Phase B)

| # | القرار | التوصية |
|---|--------|---------|
| 1 | تقسيم `files` إلى `lecture_files`/`summary_files`/`assignment_files`؟ | **لا** — الإبقاء على `files` عديم الشكل الموجود فعلاً، ووصله بالواجهة بدل إعادة هيكلته. |
| 2 | تحويل `options`/`pairs`/`items` إلى JSONB داخل `questions`؟ | **لا** — الإبقاء على الجداول الفرعية المنفصلة (متطلب أمني مثبَت، ليس تفضيلاً). |
| 3 | إصلاح فجوة عرض الملفات الفارغة في وضع القاعدة (§4.3)؟ | ✅ **نُفِّذت (Phase B)** — انظر `IMPLEMENTATION_REPORT.md`§"المرحلة 10 (Phase B — Storage + File Parity)"؛ `mapFile`/`mapLecture`/`mapSummary`/`mapAssignment` تُحلّ الآن `storage_path`/`external_url`/`public_url` بنفس شكل `DLP.data` النهائي في وضعي الملفات الثابتة والقاعدة. |
| 4 | بناء لوحة إدارة CRUD كاملة (Phase D)؟ | ✅ **نُفِّذت** — انظر `IMPLEMENTATION_REPORT.md`§"المرحلة 9 (Phase D)" للتفاصيل الكاملة. |
| 5 | إضافة بريد/كلمة مرور كميزة تسجيل دخول للمستخدمين (لا فقط اختبار CI)؟ | **لا — قرار صريح من المستخدم في Phase B**: Google OAuth يبقى وسيلة الدخول الوحيدة؛ بريد/كلمة مرور يبقى أداة اختبار CI فقط (`tests/e2e.live.js`). |
| 6 | إنشاء Bucket `course-files` والبدء برفع ملفات فعلي؟ | ✅ **نُفِّذت (Phase B)** — Bucket `course-files` عام (public=true)، 20MB/ملف، أنواع MIME محدودة (مستندات/عروض/صور شائعة، بلا فيديو)؛ 4 سياسات RLS على `storage.objects` (قراءة مفتوحة، كتابة/تعديل/حذف admin فقط عبر `is_admin_or_instructor()`)؛ انظر `010_storage_bucket_and_file_columns.sql` و`IMPLEMENTATION_REPORT.md`§"المرحلة 10". |
| 7 | حذف/تعديل `courses`؟ | لا حاجة — الإبقاء عليه كما هو. |

---

## الخلاصة والخطوة التالية

هذا تدقيق **Phase A فقط** — لم يُعدَّل أي ملف كود. القسم الأهم للموافقة هو **§4.1 و§4.2**: التصميم
المقترح في طلبك يتعارض مباشرة مع تصميم أمني قائم فعلاً وموثَّق ومُختبَر. توصيتي رفض ذلينك الجزئين
تحديداً من التصميم المقترح، وقبول بقية الخطة (Storage، CRUD إداري، توثيق موسّع) كعمل إضافي حقيقي فوق
الأساس الموجود، منفَّذ على مراحل صغيرة قابلة للاختبار.

**بانتظار موافقتك على جدول القرارات في §7 (تحديداً 1، 2، 4، 5، 6) قبل البدء في Phase B.**
