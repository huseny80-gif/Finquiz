# خطة Migration التدريجية — من الموقع الثابت إلى Supabase

خطة متعددة المراحل، كل مرحلة قابلة للاختبار بمفردها وقابلة للتراجع، دون حذف أي نظام قائم قبل
توفر بديل يعمل ومُختبر بديلاً عنه.

## المرحلة 0 — لا شيء أمامي بعد (منجزة)

- تدقيق معماري كامل (`ARCHITECTURE_AUDIT.md`).
- مخطط قاعدة البيانات الكامل مطبَّق فعلياً على مشروع Supabase حي (`001_initial_schema.sql`).
- RLS + إخفاء أعمدة الإجابات على مستوى القاعدة (`002_rls.sql`).
- دوال تصحيح وحساب النتائج من طرف الخادم (`003_functions.sql`).
- بذرة كاملة لكل المحتوى الحالي (5 مواد، 164 سؤالاً) مطابقة تماماً للمصدر (`generate-seed.js` +
  `course2.sql`)، مُطبَّقة ومُتحقَّق من مطابقة الأعداد.
- **لم يتغيّر أي ملف في `assets/js/*` أو `data/*` أو `index.html`.**

## المرحلة 1 — طبقة الوصول

**الحالة: كل البنود (1-7) منجزة ومُختبرة (64/64 وحدة + 60/60 متصفح، صفر تراجع). التفعيل الفعلي
(`enabled: true`) مؤجَّل عمداً — انظر القرار أسفل البند 7.**

1. ✅ `assets/js/core/supabase.js` — تهيئة عميل Supabase (SDK مُحمَّل محلياً من `assets/vendor/`
   وليس من CDN، بسبب `script-src 'self'` في CSP)، بمفتاح `anon`/`publishable` فقط من
   `data/config/supabase.js` — **لا سرّ في الكود المصدري**. يسقط بهدوء (`null`) بلا مكتبة/مفاتيح/
   اتصال، دون رمي أي استثناء.
2. ✅ `assets/js/core/api.js` — الطبقة الوحيدة المسموح لها بمخاطبة Supabase مباشرة (queries وRPC).
   المكوّنات (`components/*.js`) يجب ألا تستورد `supabase.js` مباشرة أبداً — فقط عبر `api.js`.
   تحوي `fetchAllContent()` (تبني شكل `DLP.data` نفسه) ودوال RPC الأربع (`startQuizAttempt`,
   `saveQuizAnswer`, `finishQuizAttempt`, `revealQuestionAnswer`) لاستخدامها في المرحلة 2. كل
   دالة تُعيد Promise وتُرفض بهدوء بلا اتصال، لا تطرح استثناءً متزامناً أبداً.
3. ✅ `assets/js/core/auth.js` — تسجيل دخول Google عبر Supabase Auth (`signInWithOAuth`)، مع إبقاء
   التصفح العام (المحتوى، الاختبارات، البحث) يعمل بلا حساب؛ الدخول مطلوب فقط لحفظ نتيجة اختبار.
4. ✅ تعديل `index.html`: أُضيف نطاق مشروع Supabase إلى `connect-src` في الـ CSP، وسكربت المكتبة
   المحلي (يبقى `script-src 'self'` لأنه ملف محلي تحت `assets/vendor/`)، و`<script>` tags
   الثلاثة الجديدة + `data/config/supabase.js` في مكانهما المناسب.
5. ✅ تعديل `assets/js/core/store.js`: أُضيف `dataSource()` (`'static'`/`'database'`) و`hydrate()`
   غير متزامنة تملأ `DLP.data`/`DLP.subjectOrder` من `api.js` عند نجاح الاتصال باستبدال ذرّي (لا
   خلط بيانات قديمة/جديدة)، وإلا **تسقط تلقائياً** على البيانات الثابتة الحالية — بلا أي تغيير في
   توقيع `subjects()`, `getSubject()`, `list()`, `stats()`, `latestUpdates()`, `findQuiz()`.
6. ✅ تعديل `assets/js/app.js`: `init()` صارت `async` وتستدعي `await DLP.store.hydrate()` كأول
   سطر فيها — **قبل** أي رسم للهيكل (Header/Sidebar يعتمدان على `DLP.store.subjects()` مباشرة،
   فكان يجب أن يسبقهما لا أن يسبق `router.start()` فقط كما كان مخطَّطاً أصلاً في هذا الملف).
7. ✅ 5 اختبارات وحدة جديدة تغطي `hydrate()`: بلا `DLP.api`، نجاح فعلي (استبدال ذرّي)، رفض
   الشبكة (fallback)، شكل استجابة ناقص (fallback)، وتأكيد أن `enabled` معطَّل افتراضياً حالياً.

### قرار مهم: `enabled: false` حتى إشعار آخر

رغم اكتمال البنود 1-6 واختبارها بعميل وهمي (mock)، **`data/config/supabase.js` يبقي
`enabled: false`** عمداً: تفعيلها الآن يعني أن `hydrate()` سينجح فعلياً على الموقع الحي ويستبدل
البيانات بأخرى قادمة من القاعدة، حيث أسئلة المطابقة (`type: 'match'`) ستصل بـ `pairs: []` (لأن
`question_pairs` محجوب عن anon في RLS الحالي — التTODO نفسه). لأن `quiz-view.js` يقرأ
`question.pairs` مباشرة بلا حارس فارغ، هذا سيكسر نوع سؤال كامل بصمت لكل زائر. **لا يجوز تفعيل
`enabled: true` قبل**: (أ) حل فجوة `question_pairs` في `002_rls.sql`، أو (ب) تعديل `quiz-view.js`
ليتجاوز/يُخفي أسئلة المطابقة بأمان حين تصل بمصفوفة فارغة، أو (ج) تعديل `api.js` ليُسقِط أسئلة
النوع `match` بالكامل من `fetchAllContent()` بدل إرسالها بشكل معطوب — أيّما يُختار، مع تحقّق فعلي
بمتصفح حقيقي متصل بالمشروع قبل التفعيل.

## المرحلة 2 — محرّك الاختبار الحقيقي

**الحالة: البند 1 مُنفَّذ بتصميم مُعدَّل عن الخطة الأصلية (انظر التبرير أدناه)، ومُختبر بالكامل
(12 اختبار وحدة جديد، 76/76 إجمالاً + 60/60 متصفح، صفر تراجع). البندان 2-3 لم يبدآ بعد.**

1. ✅ تعديل `components/quiz-view.js`: عند وجود مستخدم مسجَّل **و**اتصال جاهز، كل ضغطة "تحقق"
   تستدعي `start_quiz_attempt` (مرة واحدة لكل اختبار، بالتخزين المؤقّت في الذاكرة) ثم
   `save_quiz_answer` لتلك الإجابة تحديداً، وضغطة "إنهاء" تستدعي `finish_quiz_attempt` على نفس
   المحاولة. إعادة المحاولة (`retry`/`clear`) تُسقط المحاولة الخادمية القديمة فتبدأ محاولة جديدة
   حقيقية لا استكمالاً لمحاولة سابقة.

   **تعديل عن الخطة الأصلية**: النص الأصلي هنا قال "لا تصحيح في المتصفح لمستخدم مسجَّل — الخادم
   هو الحكم الوحيد". هذا **لم يُنفَّذ** بهذا الشكل، والسبب بنيوي لا اختياري: `core/store.js` لا
   يزال دائماً في وضع `DATA_SOURCE='static'` (لأن `enabled:false`، انظر المرحلة 1)، وبيانات
   `data/subjects/*.js` الثابتة تحمل أصلاً الإجابة الصحيحة الكاملة لكل سؤال (هذا سلوك موروث من
   قبل أي عمل Supabase بالكامل — راجع `ARCHITECTURE_AUDIT.md`§2 — ولم يتغيّر بعد لأن رفعه يتطلب
   حذف `answer` من الملفات الثابتة، وهذا يخالف صراحة قاعدة "لا تحذف `data/subjects/*.js` في هذه
   المرحلة"). التصحيح المحلي عبر `core/quiz.js` **يبقى كما هو تماماً لكل المستخدمين** (مسجَّلين
   وغير مسجَّلين على حدٍّ سواء) لأنه الوحيد المتوافق مع البيانات المتوفرة فعلياً في المتصفح
   اليوم. ما أُضيف هو **حفظ دائم إضافي بحت**: لمستخدم مسجَّل، كل إجابة يُعاد حسابها وتصحيحها
   أيضاً من طرف الخادم (بمعزل تام عن التصحيح المحلي المعروض للمستخدم) وتُخزَّن في
   `quiz_attempts`/`quiz_answers` — بديل حقيقي عن الاعتماد الحصري على `localStorage` (القاعدة
   رقم 10)، دون انتظار حل مشكلة تسريب الإجابات في البيانات الثابتة (خارج نطاق مرحلة Supabase
   هذه بحكم قاعدة عدم حذف تلك الملفات، وتُترك لقرار منفصل لاحق إن رغب المستخدم بمعالجتها).
   كل استدعاء RPC هنا **fire-and-forget** بالكامل: لا ينتظره أي تفاعل واجهة، وأي فشل شبكة يُبتلع
   بهدوء دون أي أثر ملحوظ على تجربة المستخدم.
2. ⬜ رسالة واضحة للمستخدم أن تسجيل الدخول يحفظ النتيجة بشكل دائم (تحسين واجهة — لم يبدأ).
3. ⬜ استخدام `reveal_question_answer` لعرض الشرح بعد كشف السؤال — حالياً لا يزال الشرح يأتي من
   `question.explanation` الثابت المحلي (نفس ملاحظة "التصحيح المحلي" أعلاه)، فلا حاجة فعلية بعد
   لاستدعاء هذه الدالة. تبقى مبنية وجاهزة في `api.js` من المرحلة 1.

## المرحلة 3 — لوحة الطالب

**الحالة: مكتملة بتصميم مُبسَّط عن البند الثاني (انظر أدناه)، ومُختبرة بالكامل
(11 اختبار وحدة جديد + 3 اختبارات متصفح جديدة، 87/87 + 63/63، صفر تراجع).**

1. ✅ `assets/js/components/dashboard.js` — تعرض `student_progress` الحقيقي (عبر
   `api.fetchStudentProgress`) وسجل آخر 10 محاولات (عبر `api.fetchMyAttempts`) — كلاهما محسوب من
   نشاط حقيقي في `quiz_attempts`/`quiz_answers`، لا رقم ثابت. أربع حالات عرض واضحة: (أ) القراءة
   الحية معطَّلة (`enabled:false` اليوم) → بطاقة "قريباً" بلا أي محاولة اتصال، (ب) متاحة لكن بلا
   تسجيل دخول → دعوة لتسجيل الدخول عبر Google بلا كشف أي بيانات، (ج) جارٍ التحميل، (د) خطأ شبكة
   مع زر إعادة محاولة. **لا تُعرض أي بطاقة تقدّم أو محاولة قبل تسجيل الدخول فعلياً** (مُختبر صراحةً).
2. ✅ مسار `/dashboard` جديد في `router.js`، وربطه في `layout.js` ضمن نفس نمط عناصر
   `library`/`assistant`/`certificates` الحالي (رابط دائم الظهور في التنقل، مخفي من الشريط
   العلوي المضغوط على الشاشات الواسعة كبقية تلك الروابط، ومرئي في الشريط الجانبي/قائمة الجوال).

   **تبسيط مقصود عن الخطة الأصلية**: النص الأصلي هنا اقترح أن "يظهر [الرابط] فقط لمستخدم
   مسجَّل" — أي تبديل ديناميكي لعنصر تنقّل بحسب حالة الدخول. لم يُنفَّذ هذا التبديل الديناميكي:
   الرابط يظهر دائماً في التنقل (كبقية الروابط التجريبية الحالية)، والبوّابة الفعلية على مستوى
   محتوى الصفحة نفسها لا مستوى ظهور الرابط — فتح `/dashboard` بلا تسجيل دخول يعرض دعوة لتسجيل
   الدخول بدل 404 أو رابط مخفي. هذا يطابق نمط UX الموجود فعلاً في المنصة لروابط `library`/
   `assistant`/`certificates` (روابط ظاهرة دائماً، والصفحة نفسها توضّح حالة "قيد التطوير")، ويتفادى
   تعقيد إعادة رسم الهيدر/الشريط الجانبي بالكامل عند كل تغيّر في حالة تسجيل الدخول — وهو تعقيد لا
   ثمرة عملية منه اليوم بما أن `enabled:false` يجعل كل مستخدم في نفس الحالة (غير مسجَّل) على أي حال.

## المرحلة 4 — سقالة الإدارة (بنية فقط في هذه المرحلة)

1. `assets/js/components/admin/` — هيكل ملفات فقط (لا واجهة كاملة بعد): قوائم المواد/الأسئلة
   للقراءة عبر `api.js`، بلا نماذج تحرير/حذف كاملة الوظائف في هذه المرحلة.
2. الصلاحيات الكاملة (admin/instructor يكتبون، غيرهم يقرأون فقط) **موجودة بالفعل في RLS** منذ
   المرحلة 0 — لا حاجة لأي تعديل قاعدة بيانات إضافي لدعم الإدارة لاحقاً.

## المرحلة 5 — التوثيق النهائي وتقرير التنفيذ

`IMPLEMENTATION_REPORT.md` يُحدَّث بنهاية كل مرحلة فعلية (لا في نهاية الخطة فقط) ليعكس ما تم
تنفيذه واختباره فعلياً مقابل ما هو مخطَّط، متضمّناً أي فجوة معروفة (مثل `question_pairs`).

## الاختبارات المطلوبة (تُضاف تدريجياً مع كل مرحلة، لا دفعة واحدة)

1. تهيئة إعداد Supabase (`supabase.js`) بمفاتيح صحيحة/ناقصة.
2. `DATA_SOURCE` يتحول لـ fallback تلقائياً عند فشل الاتصال أو غياب المفاتيح.
3. `store.hydrate()` تملأ نفس بنية `DLP.data` التي تنتجها `<script>` tags الثابتة (تطابق شكلي).
4. `store.subjects()/getSubject()/list()/stats()/latestUpdates()/findQuiz()` تُعيد نفس النتائج
   بعد hydration من القاعدة مقارنة بالبيانات الثابتة (في بيئة اختبار متصلة فعلياً بمشروع تجريبي).
5. قراءة كل الأنواع الستة للأسئلة من القاعدة دون أي حقل إجابة مكشوف قبل الإرسال.
6. إنشاء محاولة اختبار (`start_quiz_attempt`) لمستخدم مسجَّل تجريبي.
7. حفظ إجابة (`save_quiz_answer`) لكل نوع سؤال ومطابقة `is_correct` بمنطق `core/quiz.js` نفسه.
8. حساب `score_percent` عبر `finish_quiz_attempt` مطابقاً لحساب `quiz.js` المحلي على نفس الإجابات.
9. تحديث `student_progress` تلقائياً بعد `finish_quiz_attempt`.
10. حالة تسجيل الدخول/الخروج تعكس نفسها في الواجهة (`auth.js`) دون كسر التصفح العام.
11. افتراض RLS: مستخدم anon لا يمكنه قراءة `answer`/`is_correct`/`rubric` مباشرة عبر استعلام REST.
12. لا تسريب لإجابة أي سؤال إلى الشبكة (تبويب Network) قبل استدعاء `save_quiz_answer` لذلك السؤال.
13. عدم تراجع في أي من الاختبارات الحالية (49 وحدة + 60 e2e) بعد كل تعديل في `store.js`/`app.js`.
14. `search.js` يعمل في كلا الوضعين (بيانات محلية / بيانات مُحمَّلة من القاعدة).
15. سيناريو عدم اتصال كامل (Supabase غير قابل للوصول) لا يكسر أي صفحة — يعمل الموقع بكامل وظائفه
    القرائية كما هو الحال اليوم تماماً.
