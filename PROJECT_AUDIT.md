# PROJECT_AUDIT.md

**تاريخ الفحص:** 2026-09-10
**الفرع المفحوص:** `main` @ `9480305`
**نوع الفحص:** استكشاف فقط (Phase 1 — Inspect Only) — لم يُعدَّل أو يُحذف أي كود أو ملف.

---

## 0. خلاصة تنفيذية (Executive Summary)

المشروع الحالي هو **موقع محتوى تعليمي ثابت (Static Site)** — صفر اعتماديات إنتاجية، بدون
Build Step، بدون Backend، بدون قاعدة بيانات، بدون نظام مصادقة. يُنشر مباشرة على GitHub Pages.
جودته التقنية **عالية جدًا نسبةً لحجمه**: بنية طبقات نظيفة (Content → Data Layer → Components →
Router)، اختبارات وحدة ومتصفح تعمل فعليًا وتُشغَّل في CI على كل push، توثيق داخلي ممتاز
(README شامل)، أمان أساسي مطبّق (CSP، تهريب HTML، فلترة روابط)، ودعم RTL/عربي كامل وحقيقي
(ليس ترجمة سطحية).

**لكن الفجوة بين هذا الواقع وبين الرؤية الموصوفة في "MASTER PROMPT V2" ضخمة جدًا.** الرؤية
تطلب: تطبيقات iOS وAndroid حقيقية (ليست WebView)، Backend/API، قاعدة بيانات علائقية، نظام
مصادقة (Google Sign-In)، أدوار مستخدمين متعددة، لوحة إدارة (Admin Dashboard) لإدارة المحتوى
بدون لمس الكود، نظام إشعارات، وتتبع تقدّم طالب محفوظ في قاعدة بيانات.

**لا شيء من هذه العناصر السبعة موجود اليوم.** المشروع الحالي بُني عمدًا وبوعي كامل (موثّق في
README) كموقع Static بدون Backend — وهذا قرار معماري ناجح لحجم المحتوى الحالي (5 مواد، 14
محاضرة، 164 سؤال)، وليس نقصًا عرضيًا. تحويله إلى "Enterprise Platform + Mobile App" كما هو
موصوف هو **إعادة بناء كاملة (Full Rewrite)**، وليس "تطويرًا تدريجيًا" — وهذا يتعارض مع "القاعدة
الأهم" في الطلب نفسه (Part 0: DO NOT REBUILD FROM ZERO).

القسم 9 من هذا التقرير (Recommended Architecture) يقترح مسارًا يحاول التوفيق بين الاثنين.

---

## 1. البنية المعمارية الحالية (Current Architecture)

```
Content (data/*.js)  →  Data Layer (core/store.js)  →  Components  →  Router (hash-based)  →  DOM
```

- **نمط:** Vanilla JS، IIFE modules تُعلّق نفسها على كائن عام واحد `window.DLP`.
- **التوجيه:** Router داخلي مبني على `location.hash` (`#/subject/:id/:section`) — لا يحتاج
  خادمًا لإعادة الكتابة (rewrite rules)، يعمل من الملف مباشرة (`file://`) وعلى أي استضافة ثابتة.
- **البيانات:** كل مادة دراسية = ملف JS واحد (`data/subjects/*.js`) يُصدّر كائن `SUBJECT` عبر
  `module.exports` (متوافق مع Node لأغراض الاختبار) و`window.DLP.data[id]` (للمتصفح). هذا هو
  **Content Layer** المطلوب في PART 5 من الطلب — موجود فعلاً وبجودة جيدة.
- **الفصل بين المنطق والواجهة:** `core/quiz.js` (تصحيح الإجابات) و`core/store.js` (الوصول
  للبيانات) و`core/search.js` منفصلة تمامًا عن كود DOM، وتُختبر مباشرة بـ Node بدون متصفح ولا
  محاكاة DOM. هذا نمط جيد جدًا وقابل لإعادة الاستخدام إذا احتاج المشروع لاحقًا لواجهة أخرى
  (مثل تطبيق موبايل يشارك نفس منطق التصحيح عبر تصديره كحزمة).
- **لا يوجد Build Step:** كل ملف JS يُحمَّل مباشرة عبر `<script src>` بترتيب يدوي في
  `index.html` (طبقات: config → data → core → components → app.js). هذا قرار واعٍ وموثّق
  (README قسم 1) لتجنّب أي تعقيد أدوات بناء لا يحتاجه مشروع بهذا الحجم.

---

## 2. المكدّس التقني الحالي (Existing Technology Stack)

| الطبقة | التقنية | ملاحظات |
|---|---|---|
| اللغة | HTML5 + CSS3 + JavaScript (ES5/ES6) | متوافق مع كل المتصفحات الحديثة |
| الإطار | لا يوجد (Vanilla JS) | لا React/Vue/Angular، لا jQuery |
| الاعتماديات الإنتاجية | **صفر** | فقط Google Fonts (اختياري، الصفحة تعمل بدونه) |
| أدوات التطوير | Node.js (للاختبارات فقط) + Playwright (devDependency، اختبارات متصفح) | لا webpack/vite/babel |
| التخزين | `localStorage` فقط (تقدّم الاختبار التفاعلي، السمة، طيّ الشريط الجانبي) | لا قاعدة بيانات، لا حساب مستخدم |
| الخادم | لا يوجد Backend فعلي — `scripts/serve.js` هو خادم Node بسيط لخدمة الملفات الثابتة أثناء التطوير/الاختبار فقط | ليس API |
| الاستضافة | GitHub Pages (نشر تلقائي عبر `pages-build-deployment` عند push لـ `main`) | لا Netlify/Vercel حاليًا رغم أن الكود متوافق معهما (hash routing) |
| المصادقة | **لا يوجد** | لا Login، لا Session، لا Token |
| قاعدة البيانات | **لا يوجد** | المحتوى بيانات JS ثابتة داخل المستودع |
| الموبايل | **لا يوجد** | لا مجلد Android/iOS/Flutter/React Native |
| CI | GitHub Actions: `فحص المنصة` (بناء + اختبارات وحدة + اختبارات متصفح Playwright) على كل push/PR | يعمل فعليًا وموثوق (راجعته في PRs سابقة بهذه الجلسة) |

### ملفات CI غريبة/ميتة (Technical Debt)
- `.github/workflows/jekyll-docker.yml` — يحاول بناء الموقع بـ **Jekyll**. المشروع **ليس**
  موقع Jekyll (لا `_config.yml`، لا `Gemfile`، لا بنية `_posts`/`_layouts`). هذا ملف متبقٍّ من
  قالب ابتدائي لمستودع GitHub ولم يُستخدم أو يُحذف. يعمل بلا فائدة على كل push لـ `main` ويستهلك
  وقت CI مجانًا (Docker pull + jekyll build فارغ تقريبًا).
- `.github/workflows/generator-generic-ossf-slsa3-publish.yml` — قالب SLSA provenance عام،
  لا علاقة له بمشروع محتوى تعليمي بلا artifacts للنشر (`workflow_dispatch` / `release` فقط،
  فلا يُشغَّل تلقائيًا لكنه موجود بلا داعٍ).
- **التوصية:** حذف الملفين في PR منفصل بعد التأكيد — لا يؤثران على الوظائف الفعلية لكنهما ضجيج.

---

## 3. الميزات الموجودة فعليًا (Existing Features)

- عرض 5 مواد دراسية، كل مادة: محاضرات، ملخصات، واجبات/تمارين، اختبار تفاعلي، مراجع، ملفات
  وموارد، سجل تحديثات — بالضبط بنية `Course → Subject → {Lectures, Summaries, ...}` المطلوبة
  في PART 5.
- **محرك اختبارات تفاعلي (Quiz Engine)** حقيقي وليس Placeholder:
  - أنواع أسئلة مطبّقة فعلاً: `mcq`, `tf` (صح/خطأ), `fill` (إكمال), `match` (مطابقة),
    `order` (ترتيب), `open` (سؤال مفتوح/سيناريو بمعايير تقييم ذاتي/rubric).
  - كل سؤال يحمل: `id`, `lectureId`, `type`, `difficulty`, `prompt`, `explanation`. هذا يغطي
    معظم حقول PART 8 (Question/Options/Answer/Explanation/Difficulty/Subject عبر الملف/Topic
    ضمنيًا عبر lectureId) — الناقص: `source` و`tags` كحقول صريحة منفصلة، ولا "نقاط/Score" لكل
    سؤال (كل الأسئلة تُحسب بوزن متساوٍ).
  - فلترة حسب الصعوبة وحسب المحاضرة (تُبنى ديناميكيًا من الأسئلة الفعلية) — تصفية مركّبة، تُحفظ
    في `localStorage` وتُستعاد.
  - تجربة الاختبار: تنقّل سابق/تالي، عرض تقدّم، تصحيح فوري لكل سؤال (Check)، حساب نتيجة نهائية
    (صحيح/خطأ/غير مُجاب/نسبة مئوية)، إعادة المحاولة، مسح التقدّم المحفوظ.
  - **لا يوجد**: مؤقّت (Timer)، بنك أسئلة عشوائي (Random Selection من مجموعة أكبر)، شاشة
    "تعليمات قبل الاختبار" منفصلة بعدد الأسئلة/الوقت كما في PART 9.
- **بحث شامل** (`core/search.js`) عبر كل المواد والمحاضرات والملخصات والأسئلة والموارد، مع
  تطبيع عربي (يتجاهل التشكيل واختلاف الهمزة)، AND متعدد الكلمات، تصفية حسب المادة — هذا يغطي
  معظم PART 15 عدا الترقيم (Pagination — غير مطبّق لأن حجم النتائج صغير حاليًا، 316 عنصر).
  الفرز (Sorting) الصريح غير مطبّق كخيار للمستخدم.
- **إحصائيات المنصة** محسوبة ديناميكيًا من البيانات نفسها (عدد المواد/المحاضرات/الأسئلة...)
  — لا أرقام يدوية ثابتة، مطابق لمبدأ Content-Driven في PART 4.
- **تعدد اللغة/الاتجاه:** طبقة `i18n.js` مع جدول ترجمة كامل عربي/إنجليزي (`data/config/strings.js`،
  322 سطرًا)، دعم RTL/LTR فعلي مُختبر (اختبار مخصص "الجدول الإنجليزي مكتمل ومطابق للعربية
  مفتاحًا بمفتاح").
- **الوضع الداكن/الفاتح** (Dark Mode) مع Design Tokens منفصلة لكل وضع.
- **شريط جانبي متجاوب + شريط تنقل سفلي للموبايل** (`layout.js`) — Responsive حقيقي وليس تصغير
  Desktop، مبني من مصدر تنقّل واحد موحّد.
- **صفحات "قريبًا" صريحة وصادقة:** المساعد الذكي، المكتبة، الشهادات — كل واحدة تعرض شارة
  "ميزة قيد التطوير" واضحة، **لا تحاكي أي وظيفة أو Backend غير موجود**. هذا مطابق تمامًا لمبدأ
  PART 37 (NO FAKE PRODUCT) — نقطة قوة حقيقية في المشروع الحالي.
- **إمكانية وصول (Accessibility):** بنية دلالية (header/main/footer/nav)، `aria-live` لإعلان
  تغيّر الصفحة لقارئ الشاشة، `roving tabindex` للتبويبات، تركيز يُدار عند التنقّل، أيقونات
  زخرفية مخفية عن قارئ الشاشة — مُختبرة فعليًا (7 اختبارات إمكانية وصول في e2e).
- **أمان أساسي:** Content-Security-Policy صارمة في `index.html` (`script-src 'self'` فقط، لا
  `unsafe-inline` للسكربتات)، تهريب HTML شامل (`escapeHtml`)، فلترة بروتوكولات الروابط (رفض
  `javascript:` وغيرها)، فحص تلقائي لغياب أسرار/مفاتيح في المستودع (اختبار مخصص).

---

## 4. الصفحات الموجودة (Existing Pages)

| المسار | الصفحة |
|---|---|
| `#/` | الرئيسية — ترحيب، بطاقات المواد، آخر التحديثات، إحصائيات |
| `#/subject/:id/lectures` | محاضرات المادة |
| `#/subject/:id/summaries` | ملخصات المادة |
| `#/subject/:id/assignments` | واجبات/تمارين المادة |
| `#/subject/:id/quizzes` | الاختبار التفاعلي |
| `#/subject/:id/references` | المراجع |
| `#/subject/:id/resources` | الملفات والموارد |
| `#/subject/:id/updates` | سجل التحديثات |
| `#/search` | البحث |
| `#/about` | من نحن (فريق العمل، أرشيف الكورس الأول) |
| `#/contact` | التواصل |
| `#/library`, `#/assistant`, `#/certificates` | صفحات "قريبًا" صريحة |

كل صفحة محتوى داخل `files/<subject>/*.html` (محتوى محاضرة كامل، مسرد مصطلحات، مراجع) هي
**صفحة HTML مستقلة تمامًا** خارج نظام الـ Router أعلاه — تُفتح كرابط عادي من داخل بيانات المادة،
بقالب بصري مشترك (Navy/Gold) وشريط عودة للمنصة. هذا نمط ثنائي (Router SPA + صفحات ثابتة منفصلة)
يستحق التوحيد مستقبلًا إن كبر عدد هذه الصفحات (حاليًا 14 صفحة محاضرة + صفحات ملخص/أرشيف).

---

## 5. المكوّنات الموجودة (Existing Components)

`assets/js/components/`: `layout.js` (الأكبر، 312 سطر — الهيدر/الشريط الجانبي/التنقل السفلي/
تبديل السمة)، `subject.js` (324 سطر — كل تبويبات صفحة المادة)، `quiz-view.js` (500 سطر — أكبر
مكوّن، محرك عرض الاختبار كاملًا)، `home.js`, `about.js`, `contact.js`, `search-view.js`, وثلاثة
مكوّنات "قريبًا" (`library.js`, `assistant.js`, `certificates.js`).

لا يوجد نظام مكوّنات قابل لإعادة الاستخدام بمعنى Framework (لا Props/State إدارة رسمية) —
كل مكوّن هو دالة `render()` تُعيد سلسلة HTML، وتُدرج مباشرة في `#main`. هذا يعمل جيدًا لحجم
المشروع الحالي لكنه **لن يتوسّع بسهولة** إلى تفاعلية معقدة (نماذج تحرير إدارية متعددة الحقول،
مثلًا) بدون اعتماد مكتبة UI فعلية.

---

## 6. المحتوى الحالي (Existing Data) — أرقام دقيقة (من `scripts/validate.js`)

```
المواد: 5 | المحاضرات: 14 | الملخصات: 22 | الواجبات: 52 | الاختبارات: 5 | الأسئلة: 164
عناصر فهرس البحث: 316
```

المواد الخمس (`data/subjects/*.js`) مطابقة تمامًا لما ورد في PART 4 من الطلب:
`ai-data`, `legal-regulatory`, `cybersecurity-governance`, `innovation-project-management`,
`risk-management`. كل مادة تحمل حقل `order` صريح (ترتيب العرض مستقل عن ترتيب الملفات).

**قاعدة "لا حذف بيانات" مُحترمة بدقة عبر كامل تاريخ المستودع** (تحقّقت من ذلك يدويًا في عدة
PRs سابقة هذه الجلسة): كل محتوى تجريبي (`demo: true`) يبقى موسومًا بوضوح، ولا يُحذف عند إضافة
محتوى حقيقي — بل يُعاد ترقيمه إذا لزم الأمر مع تصحيح كل الإشارات النصية المرتبطة.

---

## 7. نظام الأسئلة (Existing Quiz System) — تفصيل إضافي

راجع القسم 3 أعلاه. الجزء غير المُغطى من PART 10 (Question Bank إداري):
- **لا توجد واجهة لإضافة/تعديل/حذف سؤال بدون تحرير كود.** كل سؤال يُضاف يدويًا كـ object
  JavaScript داخل ملف المادة. هذا هو الفارق الجوهري الوحيد عن الرؤية — البيانات نفسها منظمة
  ومهيأة تمامًا لتُدار من واجهة إدارية لاحقًا (البنية صالحة، الناقص هو طبقة التحرير فوقها).
- لا يوجد اختيار عشوائي لمجموعة فرعية من بنك أسئلة أكبر — كل أسئلة الاختبار المنشورة تُعرض
  (مع فلترة يدوية اختيارية من المستخدم حسب الصعوبة/المحاضرة).

---

## 8. الأمان، الاختبارات، النشر — بالأرقام

- **اختبارات الوحدة (Node، بدون متصفح):** 49/49 ناجحة الآن (`npm test`) — تغطي: طبقة البيانات،
  محرك التصحيح، الترجمة i18n، الأمان (XSS/روابط/أسرار)، البحث، الموجّه، بنية النشر.
- **اختبارات المتصفح (Playwright):** ~51-60 اختبارًا (حسب آخر تشغيل موثّق: 60/60 ناجحة) —
  تغطي: التنقل، الاختبار التفاعلي كاملًا، إمكانية الوصول، الاستجابة على 6 أحجام شاشة (من
  375px إلى 1920px بلا تمرير أفقي)، أخطاء الـ Console عبر كل الصفحات.
- **CI:** يعمل على كل push/PR إلى أي فرع، يشمل الوحدة والمتصفح — موثوق وسريع (دقيقة إلى دقيقتين).
- **الأمان:** لا يوجد Backend فيُلغي فئات كاملة من المخاطر (SQLi, CSRF, Session Hijacking, Auth
  Bypass) لأنها غير قابلة للتطبيق أصلًا على موقع ثابت بلا خادم تفاعلي. المخاطر القابلة للتطبيق
  (XSS عبر المحتوى، حقن روابط خبيثة، تسريب أسرار في المستودع) **مُختبرة تلقائيًا** في CI.
- **النشر:** GitHub Pages، تلقائي بالكامل عند merge إلى `main` (workflow `pages build and
  deployment` الافتراضي لـ GitHub، منفصل تمامًا عن ملفي CI المخصصين).

---

## 9. الفجوات مقابل الرؤية الموصوفة (Gap Analysis)

| متطلب من MASTER PROMPT V2 | الحالة الفعلية | الفجوة |
|---|---|---|
| Backend/API | غير موجود | كامل — يحتاج بناء من الصفر |
| Database | غير موجود (بيانات JS ثابتة) | كامل |
| Authentication (Google Sign-In) | غير موجود | كامل |
| User Roles (Admin/Student) | غير موجود (لا مستخدمين أصلًا) | كامل |
| Admin Dashboard | غير موجود | كامل |
| Content Management بدون كود | غير موجود (التحرير Manual JS) | البنية جاهزة، الواجهة غير موجودة |
| Android App | غير موجود | كامل |
| iOS App | غير موجود | كامل |
| Student Progress محفوظ (عبر أجهزة) | جزئي جدًا — `localStorage` لكل اختبار على نفس الجهاز فقط | يحتاج حساب مستخدم + DB |
| Notifications | غير موجود | كامل |
| Search | **موجود ويعمل جيدًا** | فجوة صغيرة (Pagination/Sorting) |
| Quiz Engine | **موجود ومتقدّم فعلاً** | فجوة صغيرة (Timer, Random bank, Admin UI) |
| RTL/Arabic-first | **موجود وممتاز** | لا فجوة تقريبًا |
| Accessibility | **موجود ومُختبر** | لا فجوة تقريبًا |
| Security (للسياق الحالي) | **موجود ومناسب** | لا فجوة للسياق الحالي؛ ستظهر فجوات جديدة فور إضافة Backend/Auth |
| Testing | **موجود وحقيقي** | يحتاج توسيع فور إضافة Backend |
| No Fake Product | **مُطبّق بصدق** (صفحات قريبًا صريحة) | لا فجوة |

---

## 10. البنية المعمارية المُوصى بها (Recommended Architecture)

**لا أوصي بتنفيذ الرؤية كاملة دفعة واحدة.** السبب: القفز مباشرة إلى Backend + DB + Auth +
Admin + Android + iOS لمشروع لا يملك اليوم ولا مستخدمًا واحدًا مسجّلًا هو **مخاطرة معمارية
ومالية كبيرة** (Cost Architecture — أحد الأمور التي يطلب مني الطلب نفسه التوقف والسؤال بشأنها
في PART 35). التوصية: تحوّل تدريجي بثلاث مراحل، كل مرحلة تُبقي الموقع الحالي يعمل بلا انقطاع:

**المرحلة أ — تمكين المحتوى بدون Backend حقيقي (فورًا، بدون تكلفة استضافة إضافية):**
توسيع طبقة المحتوى الحالية (`data/subjects/*.js`) بأداة CLI/Script بسيطة (Node) تُنشئ/تُعدّل
مادة أو محاضرة أو سؤال عبر أوامر أو نموذج HTML محلي يُصدر JS صالحًا — هذا يحقق "لا حاجة لتعديل
الكود يدويًا" جزئيًا دون قاعدة بيانات فعلية بعد.

**المرحلة ب — Backend خفيف + قاعدة بيانات + مصادقة (عند الحاجة الفعلية لحسابات مستخدمين):**
Backend صغير (مثل: Node/Express أو منصة كخدمة مُدارة نوع Supabase/Firebase) + قاعدة بيانات
علائقية خفيفة (PostgreSQL عبر Supabase مثلًا، أو SQLite للبداية) + Google Sign-In. الموقع
الثابت الحالي يبقى كما هو للمحتوى العام (لا حاجة لتسجيل دخول لقراءة محاضرة)، بينما تُضاف طبقة
API فقط لما يحتاج فعلًا حسابًا: تتبع التقدّم، النتائج المحفوظة، لوحة إدارة.

**المرحلة ج — تطبيقات الموبايل:** فقط بعد استقرار الـ API في المرحلة ب، لأن تطبيق موبايل حقيقي
(وليس WebView) يحتاج عقدًا (Contract) ثابتًا مع Backend، وبناءه قبل ثبات الـ API يعني إعادة
كتابته مرتين.

هذا التسلسل يحترم "القاعدة الأهم" (لا تهدم الموجود) لأنه **يضيف طبقات فوق** الموجود بدل استبداله،
ويحترم PART 26/27 ("لا تُنشئ كل الجداول/الـ APIs إن لم تكن مطلوبة فعليًا — ابدأ بالحد الأدنى").

---

## 11. استراتيجية الموبايل (Mobile Strategy)

لا يوجد أساس موبايل اليوم على الإطلاق (لا Flutter، لا React Native، لا مشروع Android/iOS
أصلي). قبل أي سطر كود موبايل، القرار الذي يحتاج توقفًا وسؤالًا صريحًا (حسب PART 35 من الطلب
نفسه، لأنه Major Product Direction وCost Architecture):

- **React Native / Flutter** (كود مشترك جزئي) مقابل **Native منفصل (Kotlin + Swift)**.
- هل التطبيق مطلوب فعليًا الآن، أم أن الموقع الحالي (يعمل ممتازًا على الموبايل عبر المتصفح،
  مُختبر على 6 أحجام شاشة) يكفي للمرحلة الحالية من المشروع؟

**لا أوصي بالبدء في تطبيق موبايل قبل استقرار Backend/API في المرحلة ب أعلاه.**

---

## 12. استراتيجية الويب (Web Strategy)

الأساس الحالي جيد جدًا ولا يحتاج إعادة بناء. التحسينات المُقترحة **ضمن البنية الحالية نفسها**
(بدون تغيير جذري):
- توحيد صفحات المحاضرات المستقلة (`files/*/lecture-*.html`) مع نظام الـ Router الرئيسي تدريجيًا
  (اختياري — النمط الحالي يعمل ولا يكسر شيئًا).
- إضافة Pagination/Sorting اختياريين للبحث عند كبر المحتوى مستقبلًا.
- حذف ملفي CI الميتين (jekyll-docker + SLSA) لتقليل الضجيج.
- توثيق `Claude.md` الفارغ الذي أُنشئ مؤخرًا على GitHub (commit `9480305`) — يحتاج تعبئة أو حذف.

---

## 13. استراتيجية الخادم (Backend Strategy)

**لا يوجد Backend اليوم بالمعنى الحرفي.** أي قرار ببناء واحد هو قرار "Major Data Model" و
"Deployment Architecture" يستوجب توقفي وسؤالك أولًا وفق PART 35 — تحديدًا:
- منصة مُدارة (Supabase/Firebase — أسرع للبدء، تكلفة تشغيل أقل في البداية، Auth جاهز) مقابل
  Backend مخصص (تحكم كامل، تعقيد أعلى، وقت تطوير أطول).
- هل الاستضافة تبقى GitHub Pages للواجهة الثابتة مع Backend منفصل (Vercel/Railway/Render)، أم
  انتقال كامل لمنصة واحدة تخدم الاثنين؟

---

## 14. استراتيجية قاعدة البيانات (Database Strategy)

عند اتخاذ القرار، الحد الأدنى الصحيح لأول إصدار (وليس كل الجداول المذكورة في PART 26 دفعة
واحدة): `users`, `roles` (أو حقل role بسيط على users)، `attempts` (محاولات الاختبار)،
`progress` (تقدّم مُلخّص لكل مستخدم/مادة). بيانات **المحتوى نفسه** (مواد/محاضرات/أسئلة) يمكن أن
تبقى كما هي في ملفات JS لفترة أطول، أو تُهاجَر لجداول `subjects/lectures/questions` فقط عند
بناء لوحة الإدارة الفعلية — لا داعٍ لهجرتها قبل ذلك.

---

## 15. خارطة طريق تنفيذية مُقترحة (Implementation Roadmap)

1. **تنظيف سريع (بدون مخاطرة):** حذف workflows الميتة، تعبئة/حذف `Claude.md` الفارغ.
2. **قرار معماري (يحتاج موافقتك):** اختيار مسار Backend (مُدار مقابل مخصص) واختيار قاعدة
   البيانات، بعد نقاش صريح للتكلفة والوقت.
3. **مصادقة + مستخدم واحد (Student) فقط:** Google Sign-In + جدول `users` + حفظ نتائج الاختبار
   بدل `localStorage` فقط.
4. **لوحة إدارة أولية:** إدارة الأسئلة فقط (أعلى قيمة، أكثر تكرارًا يدويًا اليوم) قبل التوسع
   لإدارة كل أنواع المحتوى.
5. **تتبع تقدّم حقيقي:** مبني على بيانات المحاولات الفعلية من قاعدة البيانات.
6. **تقييم الحاجة الفعلية لتطبيق موبايل** بناءً على استخدام حقيقي للموقع على الموبايل.
7. **بناء تطبيق موبايل** (إن تأكدت الحاجة) بعد استقرار الـ API.

---

## 16. المخاطر (Risks)

- **مخاطرة الإفراط الهندسي (Over-engineering):** بناء Backend/DB/Auth كاملة لمشروع لا يزال
  بلا مستخدمين مسجّلين قد يكون استثمار وقت وتكلفة في غير محله قبل التحقق من الحاجة الفعلية.
- **مخاطرة كسر الموقع الحالي:** أي تعديل في `core/store.js` أو `core/router.js` يمس كل صفحة —
  يحتاج تشغيل `npm run build` (يشمل الاختبارات) قبل أي push، كما هو معمول به فعلًا في هذه الجلسة.
- **مخاطرة ازدواجية منطق العمل (PART 31):** إن بُني تطبيق موبايل بمنطق تصحيح أسئلة منفصل عن
  `core/quiz.js` الحالي، ستُعقّد الصيانة فورًا — يجب تصدير هذا المنطق كحزمة/API مشتركة من البداية.
- **مخاطرة تكلفة الاستضافة:** الانتقال من Static/مجاني (GitHub Pages) إلى Backend+DB يُدخل
  تكلفة تشغيل شهرية متكررة — قرار له أثر مالي مباشر، يجب اتخاذه بوعي كامل وليس كتفصيل تقني.
- **مخاطرة الجدول الزمني:** حجم الرؤية الكاملة (Enterprise + Mobile ×2 + Admin + Backend) يمثّل
  أشهرًا من العمل لفريق متعدد التخصصات — تنفيذها كطلب واحد متتابع دون تجزئة سيؤدي لنتائج جزئية
  غير مكتملة في كل الجبهات بدل نتيجة كاملة في جبهة واحدة.

---

## 17. توصية نهائية

المشروع الحالي **ليس بحاجة لإعادة بناء** — إنه أساس صلب وموثّق جيدًا ومُختبر فعليًا. التوصية
هي المضي في **PART 35/36** من طلبك نفسه: التوقف هنا (كما طُلب في PHASE 1) والاتفاق معك تحديدًا
على أي من المسارات التالية تريد البدء به فعليًا، قبل كتابة أي كود Backend/Auth/Mobile:

1. البقاء ضمن Static Site وتوسيع المحتوى فقط (المسار الحالي المُثبت نجاحه طوال هذه الجلسة).
2. البدء الفعلي بالمرحلة (ب) أعلاه: Backend خفيف + Auth + قاعدة بيانات لتتبّع تقدّم طالب حقيقي،
   كأول خطوة ملموسة نحو الرؤية الكاملة.
3. نطاق آخر تحدده أنت.

**بانتظار موافقتك على خطة التنفيذ قبل أي تعديل في الكود، كما طلبت صراحة.**
