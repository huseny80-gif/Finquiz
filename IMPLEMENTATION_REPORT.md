# تقرير تنفيذ — Migration نحو Supabase

**الحالة العامة (بعد المرحلة 6 + إصلاحات CI حقيقي): `enabled: true` الآن. حُلَّت فجوتا
`question_pairs` وselect('*') المقيَّد اللتان كانتا تمنعان التفعيل، وأُعيد تصميم تصحيح الأسئلة
القادمة من القاعدة ليكون خادمياً (RPC). **CI حقيقي على GitHub Actions (PR #20) اتصل فعلياً
بالمشروع الحي لأول مرة وكشف 4 أعطال حقيقية** (ترتيب الأسئلة، شريط التقدّم، اختبار خُلط الترتيب)
— جميعها شُخِّصت من سجلّات CI الفعلية وأُصلحت، ثم أُعيد الدفع لإعادة التشغيل. 115/115 اختبار
وحدة، 65/65 اختبار متصفح محلياً (بلا شبكة حقيقية)، `npm run validate` ناجح. **لا يزال بانتظار
تأكيد CI الأخير بعد هذه الإصلاحات** — انظر §"المرحلة 6" و§"إصلاحات اكتُشفت عبر CI حقيقي" لكامل
التفاصيل. لا نماذج تحرير/حذف للإدارة — خارج نطاق ما طُلب. انظر §"ما لم يُنفَّذ بعد" للقائمة
الكاملة والصادقة لما تبقّى.**

## ما تم تنفيذه فعلياً واختباره

| العنصر | الحالة | كيف تحقّقتُ منه |
|---|---|---|
| `supabase/migrations/001_initial_schema.sql` | ✅ مطبَّق على مشروع Supabase حي (`kotbarynxzyhxhzribpf`) | `mcp__Supabase__apply_migration` أعاد `{"success":true}` |
| `supabase/migrations/002_rls.sql` | ✅ مطبَّق | نفس الشيء + `get_advisors(type: security)` لمراجعة التحذيرات |
| `supabase/migrations/003_functions.sql` | ✅ مطبَّق | نفس الشيء |
| `scripts/generate-seed.js` | ✅ يولّد SQL من `data/subjects/*.js` الحيّة عبر `tests/harness.js` (قراءة فقط، لا يعدّل أي مصدر) | تشغيل فعلي + مطابقة عدّاد الصفوف بمخرجات `stderr` |
| `supabase/seed/course2.sql` وتطبيقه | ✅ طُبِّق بالكامل على القاعدة الحية، على 14 دفعة بترتيب الاعتماديات | استعلام `count(*)` نهائي على كل الجداول الـ14 — **تطابق تام** مع الأعداد المتوقعة: `subjects=5 lectures=14 summaries=22 assignments=52 quizzes=5 questions=164 options=248 pairs=38 items=49 references=10 resources=27 updates=27 files=45` |
| `ARCHITECTURE_AUDIT.md`, `DATABASE_SCHEMA.md`, `DATABASE_SETUP.md`, `MIGRATION_PLAN.md`, `.env.example` | ✅ مكتوبة | — |
| `assets/vendor/supabase-js.min.js` | ✅ عميل Supabase JS v2.116.0 (UMD) محمَّل محلياً من سجل npm الرسمي، لا من CDN — متوافق مع `script-src 'self'` | `node -e "new vm.Script(...)"` يتحقق من صحة الصياغة؛ فحص الحجم (<1MB) ضمن `npm run validate` |
| `data/config/supabase.js` | ✅ رابط المشروع ومفتاح anon/publishable العلنيَّين فقط — لا سرّ | اختبار مخصّص يتحقق من غياب أي إشارة لمفتاح الصلاحية الكاملة |
| `assets/js/core/supabase.js` | ✅ يهيّئ العميل إن توفرت المكتبة والمفاتيح والتفعيل، ويسقط بهدوء (`null`) خلاف ذلك — لا يرمي أبداً | 5 اختبارات وحدة جديدة (بلا مكتبة / `enabled:false` / مفاتيح ناقصة / تهيئة ناجحة / تمرير المفاتيح الصحيحة للعميل) |
| `assets/js/core/api.js` | ✅ الطبقة الوحيدة المصرَّح لها بمخاطبة Supabase؛ كل دالة تُعيد Promise وتُرفض بهدوء بلا اتصال (لا استثناء متزامن) | اختبارات `isReady()`/`fetchAllContent()` بلا عميل |
| `assets/js/core/auth.js` | ✅ Google OAuth عبر Supabase Auth؛ `getUser()`/`signOut()` لا يرميان أبداً؛ التصفح العام يبقى ممكناً بلا حساب | اختبارات `getUser()`/`signInWithGoogle()`/`signOut()` بلا اتصال |
| ربط الملفات الثلاثة + CSP في `index.html` | ✅ سكربتات مضافة بعد `i18n.js` وقبل `store.js`؛ `connect-src` يسمح الآن بنطاق مشروع Supabase | `npm test` (59) + `npm run validate` + `npm run test:e2e` (60) — **صفر تراجع**، بما فيها اختبار "لا توجد أخطاء Console" الذي يثبت أن تحميل المكتبة الجديدة لا يكسر أي صفحة |

## ما تم تنفيذه فعلياً واختباره — تكملة (ربط store.js/app.js)

| العنصر | الحالة | كيف تحقّقتُ منه |
|---|---|---|
| `assets/js/core/store.js`: `dataSource()` + `hydrate()` | ✅ استبدال ذرّي لـ `DLP.data`/`DLP.subjectOrder` عند نجاح `api.fetchAllContent()`، وسقوط بهدوء (بلا رمي) عند غياب `DLP.api`، أو رفض الشبكة، أو شكل استجابة ناقص | 5 اختبارات وحدة جديدة بعميل DLP.api وهمي (mock) معزول تماماً عن باقي بيانات الاختبار المشتركة |
| `assets/js/app.js`: `init()` أصبحت `async` وتستدعي `await DLP.store.hydrate()` كأول سطر | ✅ **قبل** بناء الـ Header/Sidebar (يعتمدان على `DLP.store.subjects()` مباشرة في `layout.js`) لا فقط قبل `router.start()` | `npm run test:e2e` كامل (60/60) بلا أي تغيير سلوكي ظاهر، بما فيها اختبار خلوّ Console من الأخطاء |
| 5 اختبارات وحدة إضافية + تعديل اختبارين سابقين | ✅ (64/64 إجمالاً) | `npm test` |

## قرار enabled: false (سابقاً) — لماذا كانت معطَّلة، ولماذا صارت الآن true

كانت `data/config/supabase.js` تُبقي `enabled: false` عمداً لأن `mapQuestion()` في `api.js` كانت
تُعيد `pairs: []` لكل سؤال مطابقة (فجوة `question_pairs`)، وتفعيلها كان سيكسر نوع سؤال كامل
بصمت. هذه الفجوة، وفجوة أعمق اكتُشفت أثناء العمل عليها (`select('*')` على أعمدة محجوبة جزئياً)،
حُلَّتا بالكامل في المرحلة 6 أدناه — انظرها للتفاصيل الكاملة وأدلة التحقّق.

## المرحلة 2 — حفظ دائم لنتائج الاختبار (جزئي، بتصميم مُعدَّل)

| العنصر | الحالة | كيف تحقّقتُ منه |
|---|---|---|
| `supabase/migrations/004_fix_mcq_grading_operator.sql` | ✅ استبدال `->>0` (سلوك غير موثّق في jsonb) بـ `#>>'{}'` (موثّق ومضمون) في فرع تصحيح mcq داخل `save_quiz_answer` | تحقّق تجريبي مباشر على القاعدة الحية: `'1'::jsonb ->> 0` يُعيد `"1"` فعلاً على PostgreSQL 17.6 (سلوك صحيح لكن غير موثّق رسمياً)، مقابل `#>>'{}'` الموثّق الذي يُعطي نفس النتيجة بضمان أكيد |
| صحّة منطق التصحيح الخادمي لكل الأنواع الخمسة القابلة للتصحيح الآلي | ✅ | استعلامات SQL مباشرة على صفوف حقيقية من القاعدة الحية (`ai-q1-1` لـmcq، `ai-q1-11` لـtf، `lg-q1-3` لـfill، `lg-q1-5` لـorder، `lg-q1-4` لـmatch) — كل فرع أعاد النتيجة الصحيحة المتوقعة |
| `assets/js/components/quiz-view.js`: حفظ دائم إضافي عبر RPC لمستخدم مسجَّل | ✅ | 12 اختبار وحدة جديد (`canPersist`, `toServerResponse`, `persistAnswer`, `persistFinish`, `discardAttempt`) بعملاء `DLP.auth`/`DLP.api` وهميّين — يغطون: غياب المستخدم، غياب الاتصال، النجاح الكامل (بدء محاولة ← حفظ إجابة)، إعادة استخدام نفس المحاولة عبر أسئلة متعددة، فشل الشبكة بهدوء، `retry`/`clear` يبدأ محاولة خادمية جديدة فعلاً |
| صفر تراجع في السلوك الحالي | ✅ | `npm test` (76/76) + `npm run validate` + `npm run test:e2e` (60/60، بما فيها كل اختبارات محرّك الأسئلة الستة وحفظ التقدّم في localStorage) — enabled:false يعني أن `currentUser` يبقى `null` دائماً في المتصفح الفعلي اليوم، فكل مسار الحفظ الجديد **ساكن تماماً** في الإنتاج |

### لماذا لم يُنفَّذ "التصحيح من الخادم فقط لمستخدم مسجَّل" كما ورد أصلاً في `MIGRATION_PLAN.md`

الخطة الأصلية توقّعت أن يعتمد المستخدم المسجَّل على الخادم حصراً للتصحيح. هذا غير ممكن حالياً
لسبب بنيوي: `DATA_SOURCE` يبقى `'static'` دائماً (بحكم `enabled:false`)، وملفات
`data/subjects/*.js` الثابتة تحمل أصلاً الإجابة الصحيحة الكاملة لكل سؤال — وهذا سلوك موروث من
**قبل** أي عمل على Supabase إطلاقاً (موثّق في `ARCHITECTURE_AUDIT.md`§2)، ولا يجوز حذفه في هذه
المرحلة (يخالف قاعدة "لا تحذف `data/subjects/*.js`"). فالتصحيح المحلي عبر `core/quiz.js` **يبقى
كما هو تماماً** لكل المستخدمين. ما نُفِّذ بدلاً من ذلك: حفظ دائم إضافي بحت للمستخدم المسجَّل عبر
RPC، بمعزل عن التصحيح المعروض له، يُبقي نسخة حقيقية في `quiz_attempts`/`quiz_answers` بدل
الاعتماد الحصري على `localStorage` — وهذا يحقق جوهر قاعدة "لا تُستخدم localStorage كمخزن دائم"
دون انتظار حل تسريب الإجابات في البيانات الثابتة، الذي يبقى خارج نطاق مرحلة Supabase هذه بحكم
قاعدة عدم الحذف، ويُترك لقرار منفصل لاحق إن رغب المستخدم بمعالجته.

## المرحلة 3 — لوحة الطالب

| العنصر | الحالة | كيف تحقّقتُ منه |
|---|---|---|
| `assets/js/components/dashboard.js` | ✅ 4 حالات عرض (قريباً / دعوة تسجيل دخول / تحميل / خطأ)، تجلب `student_progress` و`quiz_attempts` (آخر 10) عبر `api.js` من المرحلة 1 دون أي كشف بيانات قبل تسجيل دخول فعلي | 11 اختبار وحدة (حالات العرض الأربع، ربط عنوان المادة/الاختبار الحقيقي بدل المعرّف الخام، سقوط بهدوء لمادة/اختبار غير موجود، تنسيق الطوابع الزمنية) |
| مسار `/dashboard` + رابط تنقّل | ✅ | 3 اختبارات متصفح جديدة (الرابط في الشريط الجانبي ينقل فعلياً، حالة "قريباً" تظهر فعلاً في متصفح حقيقي، شريط المسار صحيح) + إضافة المسار لمسح أخطاء Console |
| صفر تراجع | ✅ | `npm test` (87/87) + `npm run validate` + `npm run test:e2e` (63/63) |

### تبسيط مقصود عن الخطة الأصلية

`MIGRATION_PLAN.md` كان يقترح أن يظهر رابط لوحة الطالب في التنقل **فقط** لمستخدم مسجَّل (تبديل
ديناميكي). لم يُنفَّذ هذا: الرابط ظاهر دائماً (بنفس نمط `library`/`assistant`/`certificates`
الحالي)، والبوّابة الفعلية على مستوى محتوى الصفحة نفسها — من يفتح `/dashboard` بلا تسجيل دخول
يرى دعوة لتسجيل الدخول لا كشفاً لأي بيانات. هذا تبسيط واعٍ يتفادى تعقيد إعادة رسم الهيدر/الشريط
الجانبي عند كل تغيّر في حالة الدخول، بلا أي أثر أمني (البوّابة الحقيقية — منع كشف البيانات —
موجودة ومُختبرة في الصفحة نفسها لا في إخفاء الرابط).

## المرحلة 4 — سقالة الإدارة (قراءة فقط، بحدودها المعلنة)

| العنصر | الحالة | كيف تحقّقتُ منه |
|---|---|---|
| `supabase/migrations/005_admin_read_functions.sql`: `admin_get_quiz_questions` | ✅ تتحقّق من `is_admin_or_instructor()` وترفض غير المصرَّح له 42501، ثم تُعيد كل أسئلة الاختبار كاملة الأعمدة (`answer`/`rubric`/`explanation`/`is_correct` لكل خيار) | استعلام SQL مباشر على القاعدة الحية أعاد 29 سؤالاً لـ `ai-q1` بكامل الأعمدة — مطابق للعدد المعروف |
| `supabase/migrations/006_harden_rpc_grants.sql` | ✅ ضيّق EXECUTE من `anon` على 5 دوال RPC كان بإمكانها استدعاؤها فعلياً (رغم رفضها داخلياً) رغم `revoke ... from public` السابق — انظر الشرح الكامل في `DATABASE_SCHEMA.md`. سحب EXECUTE كليّاً من anon/authenticated عن `handle_new_user`/`recompute_student_progress` (داخليتان بحتتان) | تحقّق مباشر عبر `information_schema.role_routine_grants` قبل/بعد + `get_advisors` (تحذيرات anon-callable من 9 إلى 2، المتبقيتان مقصودتان وموثّقتان) |
| `assets/js/components/admin/index.js` + `questions.js` | ✅ 4 حالات عرض (قريباً/دعوة دخول/رفض صلاحية/تحميل+خطأ)؛ قراءة فقط بالكامل، لا نموذج تحرير أو حذف | 8 اختبارات وحدة (حالات العرض، `correctAnswerLabel` لكل نوع سؤال، `renderQuestionRow` لا يرمي على سؤال بلا خيارات) |
| مساران `/admin`، `/admin/quiz/:quizId` | ✅ بلا رابط تنقّل عام عمداً (يُوصل إليهما مباشرة بالرابط فقط) | اختبارا متصفح جديدان + إضافتهما لمسح أخطاء Console |
| صفر تراجع | ✅ | `npm test` (95/95) + `npm run validate` + `npm run test:e2e` (65/65) |

## المرحلة 5 — التوثيق والتحقّق (منجزة، سابقة على المرحلة 6)

وحّدت هذه المرحلة التقرير في نسخة واحدة متسقة وأضافت أول قائمة تحقّق فعلية (migrations، تطابق
صفوف، صلاحيات RPC، نتائج الاختبارات، نظافة git) — كانت `enabled` لا تزال `false` حينها. كل تلك
الأدلة أُعيد التحقّق منها بعد المرحلة 6 (انظر القائمة النهائية أسفل هذا القسم).

## المرحلة 6 — تفعيل القراءة الحية من القاعدة (`enabled: true`)

هذه المرحلة حلّت الفجوتين الحقيقيتين اللتين كانتا تمنعان التفعيل، واكتشفت وحلّت فجوة ثالثة أعمق
لم تكن موثّقة سابقاً، ثم أعادت تصميم تصحيح الأسئلة القادمة من القاعدة، وأخيراً فعّلت `enabled: true`.

| العنصر | الحالة | كيف تحقّقتُ منه |
|---|---|---|
| `007_safe_match_pairs_and_public_check.sql`: `get_match_pairs(question_id)` | ✅ تعيد `left`/`right` كمصفوفتين منفصلتين تماماً بلا ترابط (`left` مرتّبة بـ`position`، `right` بترتيب عشوائي مستقل)، ممنوحة لـ`anon`/`authenticated` | استعلام مباشر على `lg-q1-4` الحية طابق محتوى `question_pairs` تماماً؛ `check_answer` بنفس السؤال أعاد `true`/`false` بشكل صحيح لإجابة صحيحة/خاطئة |
| نفس الملف: `check_answer(question_id, response)` عامة بلا حفظ | ✅ تصحيح فوري بلا تسجيل دخول، لا تكشف إلا صح/خطأ لإجابة مُرسلة صراحةً — نفس مستوى الكشف الذي توفّره أصلاً البيانات الثابتة لكل زائر اليوم، لا أكثر | اختبار مباشر على `ai-q1-1` (mcq) و`lg-q1-4` (match) على القاعدة الحية |
| اكتشاف: `_grade_response` الداخلية مُنحت execute لـanon/authenticated افتراضياً رغم `revoke ... from public` (نفس اكتشاف 006 بالضبط) | ✅ أُصلح بـ`008_harden_grade_response_grant.sql` | `information_schema.role_routine_grants` قبل/بعد — لا سطر لـanon/authenticated بعد الإصلاح |
| `009_extend_reveal_for_match_and_order.sql` | ✅ `reveal_question_answer` كانت تُعيد `answer=null` دائماً لأنواع match/order (لم يُكتشف من قبل لأنها لم تُستخدم فعلياً بعد) — وسِّعت لتُعيد `pairs`/`items` الصحيحة أيضاً، مع إبقاء القيد الأصلي (`authenticated` فقط) | استعلامات SQL مباشرة على `lg-q1-4` (match) وسؤال order — كلاهما أعاد الترتيب/الأزواج الصحيحة |
| اكتشاف أعمق: `api.js` كان يستخدم `select('*')` على `questions`/`question_options`/`question_items` رغم أن هذه الجداول محجوبة أعمدة منها جزئياً | ✅ تحقّق تجريبي مباشر (`set role anon; select * from questions ...`) أثبت أن هذا كان سيفشل بـ`42501` فعلياً على أي اتصال حقيقي — لم يُكتشف من قبل لأن كل الاختبارات السابقة استخدمت عميلاً وهمياً يتجاهل قيود الأعمدة | استعلامات SQL مباشرة بدور `anon` على القاعدة الحية — فشلت فعلاً كما تنبّأت، قبل أي إصلاح |
| إصلاح `api.js`: أعمدة صريحة تطابق الممنوح بالضبط + استدعاء `get_match_pairs` لكل سؤال مطابقة + إسقاط `.order('position')` عن `question_items` (العمود نفسه محجوب) مع خلط العناصر في العميل | ✅ | اختبار وحدة جديد ببناء محاكي استعلامات (`fakeColumnCheckingClient`) يحاكي قيود الأعمدة الفعلية ويفشل عمداً لو طُلب عمود غير ممنوح — واختبار "تحكّم" يثبت أن المحاكي يكتشف الخطأ فعلاً لو أُعيد `select('*')` |
| إعادة تصميم `quiz-view.js`: تصحيح خادمي (`remoteMode`/`checkRemote`/`gradedFor`/`remoteScore`) للمحتوى القادم من القاعدة | ✅ لمستخدم مسجَّل: `save_quiz_answer` (يحفظ ويصحّح) ثم `reveal_question_answer` لعرض الإجابة الصحيحة والشرح. لغير المسجَّل: `check_answer` فقط (صح/خطأ بلا كشف تفاصيل). سؤال مفتوح: لا تصحيح آلي، كشف فقط. فشل شبكة: يُعاد السؤال قابلاً لإعادة المحاولة فوراً. الوضع الثابت (`enabled` معطَّلة أو hydrate فشلت) **يبقى بلا أي تغيير** — نفس `DLP.quiz.grade()`/`score()` المحليين تماماً | 13 اختبار وحدة جديد (`remoteMode`, `checkRemote` بحالاته الأربع، `gradedFor`, `correctAnswerFromRevealed` لكل نوع سؤال، `remoteScore`, `scoreFor`) |
| اكتشاف وإصلاح: `init()` في `app.js` كانت تنتظر (`await`) `hydrate()` قبل أي رسم للهيكل — بلا مشكلة عندما `enabled:false` (تُحسم فوراً)، لكن عند التفعيل الفعلي يعني ذلك تعليق الصفحة فارغة لثوانٍ ريثما يُحسم اتصال Supabase (بطيء أو محجوب) | ✅ أُعيد التصميم: رسم فوري بالبيانات المتاحة أولاً، ثم `hydrate()` في الخلفية بلا انتظار، وإعادة رسم الهيكل والمسار الحالي فقط عند نجاحها فعلاً | اختبار متصفح فعلي كشف الانهيار (h1 مفقود، header مفقود، فشل رابط التخطي) قبل الإصلاح، ونجاح الاختبارات نفسها بعده |
| اكتشاف وإصلاح إضافي: `hydrate()` بلا مهلة زمنية — اتصال عالق (لا خطأ فوري ولا نجاح) كان يُبقي الموقع منتظراً بلا حد زمني | ✅ أُضيفت مهلة 6 ثوانٍ (`withTimeout`) تضمن سقوطاً على البيانات الثابتة مهما بطؤ الاتصال أو عَلِق | اختبار وحدة جديد بوعد لا يُحسم أبداً — يتحقّق أن `hydrate()` تُحسم خلال أقل من 10 ثوانٍ لا أن تُعلَّق للأبد؛ اكتُشف فعلياً حين علق تشغيل e2e الأول بعد التفعيل حتى انتهت مهلة 180 ثانية |
| `data/config/supabase.js`: `enabled: true` | ✅ | — |
| تحديث 3 اختبارات متصفح كانت تفترض `enabled:false` (لوحة الطالب/الإدارة تُظهر "قريباً") لتعكس الواقع الجديد (دعوة تسجيل دخول، لا كشف بيانات) | ✅ | `npm run test:e2e` |
| تحديث اختبار "لا توجد أخطاء Console" ليتجاهل فشل شبكي على مستوى النقل (`net::ERR_...`) تجاه Supabase تحديداً — متوقَّع في بيئة هذه الجلسة المحجوبة الشبكة، وليس عطلاً برمجياً | ✅ | نمط ضيّق مُحدَّد (`Failed to load resource:\s*net::ERR_`) لا يُخفي أي خطأ آخر |
| صفر تراجع بعد كل هذا | ✅ **113/113** اختبار وحدة، **65/65** اختبار متصفح، `npm run validate` ناجح — الثلاثة نُفِّذت فعلياً بعد كل الإصلاحات أعلاه لا قبلها | `npm test` + `npm run test:e2e` + `npm run validate` |

### ماذا يعني هذا عملياً الآن

القراءة الحية من القاعدة **مفعَّلة فعلياً**. أي زائر متصل بالإنترنت (لا شبكة محجوبة كهذه الجلسة)
سيحصل على محتوى من Supabase مباشرة بعد تحميل الصفحة (رسم فوري بالبيانات المتاحة، ثم تحديث صامت
عند نجاح `hydrate()`). التصحيح للمحتوى القادم من القاعدة يمر عبر RPC خادمي حصراً — لا إجابة تصل
للمتصفح قبل أن يرسلها المستخدم فعلياً. لمستخدم غير مسجَّل: صح/خطأ فقط. لمستخدم مسجَّل: صح/خطأ +
الإجابة الصحيحة + الشرح، وحفظ دائم في `quiz_attempts`/`quiz_answers`. المحتوى الثابت
(`data/subjects/*.js`) يبقى fallback حقيقي يعمل بلا أي تغيير عند أي فشل اتصال.

## إصلاحات اكتُشفت عبر CI حقيقي (بعد دفع المرحلة 6)

أول تشغيل CI فعلي بعد `enabled: true` اتصل حقيقةً بمشروع Supabase الحي (شبكة GitHub Actions
غير محجوبة، خلافاً لبيئة تطوير هذه الجلسة) — وهذا بالضبط التحقّق الحي الذي لم يكن ممكناً محلياً.
كشف 4 أعطال حقيقية، جميعها شُخِّصت من سجلّات CI مباشرة وأُصلحت:

| العطل المُكتشَف | السبب الجذري | الإصلاح | كيف تحقّقتُ |
|---|---|---|---|
| `.match-select`/أسئلة أخرى لا تظهر في مكانها المتوقَّع أثناء التنقّل بـ"التالي" | جدول `questions` بلا عمود ترتيب صريح، واستعلام `api.js` بلا `order()` — القاعدة الحية لا تضمن أي تسلسل معيَّن لصفوفها المُعادة، وفعلياً لم يطابق تسلسل `data/subjects/*.js` الأصلي (معرّفات مثل `rm-q1-10` قد تُعاد قبل `rm-q1-2`) | ترتيب الأسئلة في `api.js` صراحةً برقم تسلسلها المُستخرَج من نهاية المعرّف (`questionSeq()`) بدل الاعتماد على ترتيب القاعدة الخام | تحقّق مباشر أن 164/164 معرّف سؤال على القاعدة الحية يطابق النمط `-\d+$`؛ اختبار وحدة جديد بمحاكي يُعيد الصفوف بترتيب معكوس عمداً ويتحقّق من تصحيحه |
| "شريط التقدّم يتحدّث مع الإجابة" فشل | `remoteScore()` كانت تحسب "answered" فقط من `state.remote` (يُملأ بعد تصحيح خادمي ناجح) — فاختيار إجابة بلا ضغط "تحقّق" لا يُحرّك الشريط، خلافاً للوضع الثابت الذي يتحرك فور الاختيار | إضافة `hasResponse()` تعكس "هل اختار المستخدم إجابة" بمعزل عن معرفة الصحة (تماماً كمنطق `answered` في `core/quiz.js`) — `remoteScore()` تستخدمها لـ`answered`/`gradableAnswered`، وتبقي `correct` حصراً من تصحيح خادمي فعلي | اختبار وحدة جديد يتحقّق من `answered=4` فور اختيار إجابات لأربعة أسئلة بلا أي `state.remote`، و`correct` تبقى تعكس فقط ما صُحِّح خادمياً |
| اختبار "سؤال الترتيب" فشل ("الترتيب المبدّل يجب أن يكون خاطئاً") | `mapQuestion()` تخلط عناصر `order` عشوائياً (أمان — لا تكشف الترتيب الصحيح مسبقاً)، فافتراض الاختبار القديم "الترتيب الافتراضي صحيح دائماً" لم يعد صالحاً لمحتوى قادم من القاعدة | تحديث الاختبار ليتحقّق من آلية العمل (ظهور تغذية راجعة صريحة صح/خطأ بعد "تحقّق") بدل افتراض نتيجة مُحدَّدة سلفاً | `npm run test:e2e` |
| اختبار تصحيح Console كان سيحجب أخطاء غير متعلقة (تحقّق إضافي، لا عطل) | — | تضييق نمط تجاهل فشل الشبكة ليقتصر على `Failed to load resource: net::ERR_...` فقط | مراجعة يدوية للنمط |

**115/115** اختبار وحدة، **65/65** اختبار متصفح محلياً (بلا شبكة حقيقية في هذه البيئة) بعد هذه
الإصلاحات، ثم دُفعت وأُعيد اختبارها فعلياً على CI حقيقي مجدداً — وهذا كشف جولة ثانية من أعطال:

### الجولة الثانية (بعد الدفع الأول لهذه الإصلاحات)

الإصلاحات الثلاثة أعلاه نجحت فعلياً (شريط التقدّم وسؤال الترتيب مرّا)، لكن ظهرت أعطال جديدة:

| العطل المُكتشَف | السبب الجذري | الإصلاح |
|---|---|---|
| "تقدّم الاختبار يُحفظ بعد إعادة التحميل" فشل بخطأ جديد (لا timeout كسابقاً) | هذا الاختبار تحديداً يستخدم `page.reload()` الحقيقية (لا `open()` التي تمسح localStorage قبل كل اختبار) — بعد إعادة التحميل، `load()` تستعيد `checked=true` من localStorage لكن `state.remote` (نتيجة التصحيح الخادمية المرتبطة) **غير مُخزَّنة إطلاقاً** فيها عمداً. فتحاول `gradedFor()`/`renderQuestion` قراءة `graded.correct` من `null` فتنهار عملية الرسم | `load()` لا تستعيد `checked` إطلاقاً حين `remoteMode()` — تبقى الإجابة المُختارة (`responses`) مُستعادة كما هي؛ يُطلب "تحقّق" جديد بسيط (رخيص وآمن) بدل الاعتماد على حالة قديمة فقدت مرجعها. الوضع الثابت لا يتأثر إطلاقاً |
| اختبار "تقدّم الاختبار يُحفظ..." يفترض عالمياً أن `checked`/التغذية الراجعة تُستعادان بعد إعادة التحميل | نفس القرار أعلاه يجعل هذا غير صحيح للوضع القادم من القاعدة | الاختبار الآن يتفرّع فعلياً حسب `DLP.store.dataSource()`: في `database` يتحقق فقط من استعادة الإجابة المُختارة (`aria-pressed`)؛ في `static` يبقى السلوك القديم (فحص التغذية الراجعة) دون أي تغيير |
| "البحث العام يعيد نتائج ذات صلة" فشل مرة، ثم نجح في التشغيل التالي بلا أي تعديل | عطل عابر (flake) — نفس النمط الملحوظ سابقاً في هذا المستودع على هذا الاختبار تحديداً | لا إصلاح شيفرة؛ **تأكَّد فعلاً كونه عابراً** حين نجح الاختبار في تشغيل CI التالي بلا أي تغيير في شيفرته |

أُضيف اختباران جديدان (`load()`/`getState()`) يحاكيان `localStorage` فعلياً (كائن وهمي حقيقي، لا مجرد تجاوز) للتحقق من هذا السيناريو تحديداً في كلا الوضعين. **117/117** اختبار وحدة، **65/65** اختبار متصفح محلياً بعد هذه الجولة. دُفعت وأُعيد اختبارها على CI حقيقي مجدداً.

### الجولة الثالثة — العطل الحقيقي وراء "سؤال المطابقة"

بعد الجولتين السابقتين، بقي عطل واحد فقط ثابتاً: `.match-select` لا يظهر أبداً (نفس timeout). التحقيق كشف السبب الحقيقي (لا علاقة له بـ`localStorage` كما افترضتُ خطأً في الجولة الثانية — `open()` في `tests/e2e.js` تمسح `localStorage` قبل كل اختبار أصلاً):

`app.js`: `init()` ترسم الهيكل فوراً بالبيانات الثابتة، ثم تستدعي `hydrate()` في الخلفية، وعند نجاحها تُعيد رسم المسار الحالي. لكن إن كان المستخدم قد فتح صفحة اختبار **قبل** نجاح `hydrate()` (وهذا بالضبط ما يحدث في اختبار المطابقة — ثلاث ضغطات "التالي" سريعة فور تحميل الصفحة، قبل اكتمال `hydrate()` التي تتضمن نداءات RPC حقيقية لكل سؤال مطابقة عبر شبكة حقيقية)، فإن `quiz-view.js` يكون قد بنى ونسخ حالة الاختبار (`states[quiz.id]`) في الذاكرة اعتماداً على كائنات الأسئلة **الثابتة القديمة** (بشكل `pairs` المترابط). حين تنجح `hydrate()` لاحقاً وتُستبدل `DLP.data` بالكامل، تصبح `remoteMode()` تُعيد `true` عالمياً — لكن كائن الحالة المخزَّن في الذاكرة لا يزال يشير إلى نفس أسئلة الاختبار القديمة نفسها (بلا `pairsLeft`/`pairsRight` إطلاقاً)، فتحاول `renderMatch()` قراءة `question.pairsLeft` من كائن لا يحملها فتُنتج **صفراً من الصفوف** — لا ينهار الرسم هذه المرة، بل يُنتج ببساطة قائمة أزواج فارغة بلا أي عنصر `<select>` على الإطلاق.

**الإصلاح الأول (نجح جزئياً، ثم كشف سباقاً جديداً)**: عند نجاح `hydrate()`، استدعاء `DLP.quizView.resetStates()` قبل إعادة الرسم — أصلح فعلاً اختبار المطابقة (تحقّقتُ عبر CI: نجح)، لكنه أدخل عطلاً جديداً: تصفير الحالة بالكامل يقطع الرابط بين وعد `checkRemote()` **الجاري فعلاً** وحالته لو نجحت `hydrate()` في تلك اللحظة بالذات — ظهر هذا فوراً في نفس تشغيل CI التالي: سؤال ترتيب بقي عالقاً على "⏳ جارٍ التحقق..." للأبد (الوعد استمر يعمل على كائن حالة أصبح "يتيماً" بعد التصفير، بينما الصفحة المُعاد رسمها من الصفر لا تعرف عنه شيئاً).

### الجولة الرابعة — استبدال بالمعرّف بدل تصفير كامل

**الإصلاح النهائي**: بدل `resetStates()` (تصفير شامل)، دالة جديدة `refreshAllQuestionObjects()` في `quiz-view.js` تُحدِّث فقط **كائنات الأسئلة** المخزَّنة داخل كل حالة اختبار قائمة في الذاكرة — بمطابقة المعرّف (`id`) — بينما تُبقي **نفس كائن الحالة حيّاً بلا استبدال**: `index`، `responses`، `checked`، و`state.remote` (بما فيها أي `pending: true` قيد الانتظار فعلياً) كلها تبقى كما هي تماماً؛ فقط `state.questions[i]` يُستبدَل بالنسخة الجديدة القادمة من القاعدة (بنفس المعرّف). الاستبدال بالمعرّف آمن لأن ترتيب الأسئلة بين المصدرين مضمون التطابق (`questionSeq()` من الجولة السابقة). وعد `checkRemote()` الجاري يستمر بالعمل على نفس الكائن الحيّ ويُحدِّثه بنجاح متى ما اكتمل، ويظهر التحديث فوراً لأن نفس الكائن هو ما يُعاد رسمه.

اختباران جديدان (`refreshQuestionObjects`) يتحقّقان: (أ) تحديث الأسئلة مع إبقاء `index`/`responses`/`remote.pending` سليمة تماماً، (ب) عدم الانهيار لاختبار لم يُفتح بعد (لا كائن حالة له أصلاً). **119/119** اختبار وحدة، **65/65** اختبار متصفح محلياً. هذا الإصلاح (كسابقه) غير قابل للاختبار بمحاكاة كاملة لـ`app.js`/`hydrate`/سباق التوقيت الفعلي — التحقّق الوحيد الحقيقي هو تشغيل CI التالي على الفرعين معاً (اختبار المطابقة واختبار الترتيب).

هذا الإصلاح حسّن الوضع (اختبار المطابقة تجاوز الـ timeout فعلاً هذه المرة) لكن كشف تشغيل CI التالي عطلين جديدين مختلفين تماماً: اختبار المطابقة فشل بتصحيح خاطئ ("تصحيح المطابقة" — الأزواج ظهرت واختيرت لكن التغذية الراجعة أعطت نتيجة غير متوقَّعة)، واختبار الترتيب فشل بعودة الـ timeout القديم (`.order-item` لم تظهر إطلاقاً هذه المرة، لا مجرد تعليق تصحيح).

### الجولة الخامسة — إزالة إعادة الرسم القسرية بالكامل

بعد أربع جولات من محاولة **إصلاح** إعادة الرسم القسرية (`renderShell()` + `router.navigate()` عند نجاح `hydrate()`) بطرق مختلفة، اتضح أن المشكلة الجذرية ليست في *كيفية* الحفاظ على الحالة أثناء إعادة الرسم، بل في **إعادة الرسم القسرية نفسها**: أي استبدال كامل لـ DOM أثناء تفاعل مستخدم جارٍ (اختيار من قائمة منسدلة، انتظار "تحقّق") يقطع مقابض العناصر (element handles) التي يعمل عليها ذلك التفاعل — بصرف النظر عن مدى دقة الحفاظ على البيانات المنطقية خلفه.

**الإصلاح النهائي**: حذف `renderShell()` + `router.navigate()` بالكامل من مسار نجاح `hydrate()`، والاكتفاء بـ `refreshAllQuestionObjects()` وحدها (تحديث البيانات الخلفية فقط، بلا لمس أي DOM). الصفحة المفتوحة حالياً تبقى بصرياً كما هي حتى أول تفاعل تالٍ من المستخدم (تالٍ/تحقّق/تصفية) — وحينها تُستدعى دوال الرسم (`renderQuiz`/`renderQuestion`) طبيعياً كجزء من معالج ذلك الإجراء نفسه، فتلتقط البيانات المُحدَّثة فعلياً بلا أي حاجة لإعادة رسم استباقية. أي صفحة/تنقّل جديد يُبنى دائماً من `DLP.data` المُحدَّثة. الكلفة الوحيدة: صفحة مفتوحة فعلاً لحظة نجاح `hydrate()` قد تُظهر بصرياً محتوى ثابتاً قديماً لفترة قصيرة إضافية حتى أول تفاعل — تنازل آمن مقابل إزالة فئة كاملة من السباقات المُثبَتة عبر CI حقيقي أربع مرات متتالية.

**119/119** اختبار وحدة، **65/65** اختبار متصفح محلياً بعد هذا التبسيط. التحقّق الحقيقي (كسابقيه) عبر CI على PR #20.

### الجولة السادسة — التشخيص كشف أن إزالة إعادة الرسم القسرية لم تكن السبب الجذري، وقرار حذف الاختبارات الثلاثة غير المستقرة

تشغيل CI التالي على تغيير الجولة الخامسة (حذف إعادة الرسم القسرية بالكامل) أظهر **نفس العطلين تماماً** رغم عدم وجود أي إعادة رسم قسرية على الإطلاق: اختبار المطابقة ما زال يفشل بتصحيح خاطئ، واختبار الترتيب ما زال يفشل بعدم ظهور `.order-item` إطلاقاً. هذا نفى تجريبياً فرضية أن إعادة الرسم القسرية (أو سباق توقيتها) هي السبب الجذري لهذين العطلين تحديداً — أربع جولات متتالية من محاولات الإصلاح حول هذه الفرضية لم تُصلح شيئاً فعلياً لهذين الاختبارين بالذات (وإن كانت أصلحت أعطالاً أخرى حقيقية اكتُشفت في الطريق: ترتيب الأسئلة، تزامن `checked`/`state.remote`).

أُضيفت عندئذ أدوات تشخيص مباشرة داخل الاختبارين (التقاط `dataSource()`، القيم المختارة فعلياً مقابل الخيارات المتاحة، أخطاء Console الجديدة، ومحتوى DOM عند الفشل) بهدف الحصول على بيانات فعلية من CI الحقيقي بدل الاستمرار في تخمين السبب من قراءة الشيفرة فقط — التحقّق المباشر عبر SQL أثبت أن منطق التصحيح في القاعدة صحيح تماماً لنفس القيم التي يرسلها الاختباران (`check_answer` أعادت `true` للإجابات الصحيحة المرسَلة، و`question_items` تحتوي بيانات مرتّبة وكاملة)، وقراءة متأنية لشيفرة `api.js`/`quiz-view.js` (`mapQuestion`، `renderMatch`، `renderOrder`) لم تكشف أي عطل واضح على الورق — أي أن السبب الجذري متعلّق على الأرجح بتوقيت حقيقي (RPC حقيقي لكل سؤال مطابقة عبر شبكة حقيقية، تسلسل نقرات "التالي" السريع فور تحميل الصفحة) لا يمكن استنساخه في بيئة هذه الجلسة (الشبكة إلى `supabase.co` محجوبة هنا).

بعد خمس جولات كاملة من الإصلاح-ثم-الاختبار-عبر-CI-الحقيقي بلا نجاح لهذين الاختبارين تحديداً، وطلب صريح من المستخدم بحذفها، اتُّخذ القرار التالي: **حذف الاختبارين ("سؤال المطابقة يعمل عبر القوائم المنسدلة" و"سؤال الترتيب يعمل عبر أزرار التحريك") بالكامل من `tests/e2e.js`**، إضافة إلى اختبار ثالث مرتبط ("تقدّم الاختبار يُحفظ بعد إعادة تحميل الصفحة") كان قد أصبح بدوره مصدر عدم استقرار مشابه بعد تفعيل `enabled:true` (يعتمد على تفاعل توقيتي بين `hydrate()` وحفظ الحالة عبر إعادة تحميل حقيقية). هذا **تراجع عن تغطية اختبارية مباشرة** لهذه السيناريوهات الثلاثة تحديداً في متصفح حقيقي — لا عن الوظيفة نفسها في الشيفرة (منطق المطابقة/الترتيب/استعادة التقدّم في `quiz-view.js`/`api.js` لم يتغيّر، ولا يزال مغطّى باختبارات وحدة مباشرة: `renderMatch`/`renderOrder`/`gradedFor`/`load()`/`getState()` في `tests/run.js`). التوثيق هنا صريح بذلك: **لا يوجد حالياً اختبار متصفح حي يمر فعلياً بتدفّق المطابقة أو الترتيب أو استعادة التقدّم بعد إعادة التحميل عبر القراءة الحية من القاعدة** — أي عطل مستقبلي في هذه التدفّقات الثلاثة تحديداً (في وضع `database`) لن يُكتشف تلقائياً عبر `npm run test:e2e`.

**62/62** اختبار متصفح محلياً بعد هذا الحذف (كانت 65)، **119/119** اختبار وحدة (بلا تغيير — لم يُمسّ أي اختبار وحدة)، `npm run validate` ناجح.

### الجولة السابعة — عطل حقيقي مختلف تماماً: اختبارات أساسية (mcq/tf/fill) فشلت بعد الحذف لأن التصحيح صار غير متزامن

تشغيل CI التالي (على نفس الدفعة التي حذفت الاختبارات الثلاثة أعلاه) كشف عطلاً **جديداً ومختلفاً تماماً**، في ثلاثة اختبارات أساسية لم تفشل قط من قبل: "اختيار إجابة خاطئة يُظهر الإجابة الصحيحة" (فشل: "لم تُرصد الإجابة الخاطئة")، "سؤال صح/خطأ يعمل" (فشل: "تصحيح صح/خطأ")، و"سؤال إكمال الفراغ يقبل الإجابة النصية" (فشل: `timeout` 30 ثانية على `.fill-input`).

السبب الجذري (بعد قراءة `checkRemote()`/`renderQuestion()` في `quiz-view.js`): هذه الاختبارات جميعها كُتبت أصلاً لتصحيح **متزامن** (الوضع الثابت، `DLP.quiz.grade()` يُعيد النتيجة فوراً بلا انتظار). لكن حين تنجح `hydrate()` فعلاً بحلول لحظة تشغيل الاختبار (وهذا يحدث أحياناً في CI الحقيقي بشبكة فعلية، بخلاف بيئة التطوير المحجوبة عن الشبكة حيث يبقى الوضع ثابتاً دائماً)، يتحوّل `remoteMode()` إلى `true` ويصبح التصحيح عبر RPC حقيقي **غير متزامن** — الضغط على زر "تحقّق" يعرض فوراً حالة مؤقتة "⏳ جارٍ التحقق..." (`quiz.checking`) قبل استبدالها بالنتيجة الفعلية بعد اكتمال RPC. الاختبارات الثلاثة كانت تقرأ `.feedback` **مباشرة بعد الضغط بلا انتظار**، فتلتقط أحياناً هذه الحالة المؤقتة بدل النتيجة، وتفشل التوقعات. عطل `.fill-input` تحديداً كان على الأرجح نتيجة تراكمية: فشل الاختبار السابق ("سؤال صح/خطأ") ترك السؤال الثاني عالقاً على "جارٍ التحقق" لحظة انتهاء ذلك الاختبار — لكن بما أن كل اختبار يفتح صفحة جديدة تماماً عبر `open()` (تصفح + مسح `localStorage` + إعادة تحميل)، هذا تحديداً غير مرجَّح، والتفسير الأدقّ أن `hydrate()` نفسها استغرقت وقتاً غير معتاد في تلك اللحظة بعينها من تشغيل CI فعطّل تسلسل الانتقال — بأي حال، الإصلاح أدناه يعالج فئة العطل كاملة بصرف النظر عن التفسير الدقيق للحالة الثالثة.

**الإصلاح**: دالة مساعدة موحّدة جديدة `check()` في `tests/e2e.js` تضغط زر "تحقّق" ثم تنتظر فعلياً (`waitForFunction`, مهلة 10 ثوانٍ) حتى يحمل `.feedback` صنف `show` **ولا** يحتوي نص "جارٍ التحقق" — في الوضع الثابت هذا الانتظار ينتهي فوراً (لا فرق ملحوظ)، وفي الوضع القادم من القاعدة ينتظر فعلياً اكتمال RPC. استُبدلت كل نداءات `page.click('[data-quiz-action="check"]')` تقريباً في الملف (باستثناء استخدام واحد لقراءة تسمية الزر فقط بلا ضغط تصحيح) بهذه الدالة الموحّدة، بما فيها الاختبار الذي يستخدم صفحة `blind` منفصلة (يقبل الدالة معامل صفحة اختياري).

**62/62** اختبار متصفح محلياً (بلا تغيير ظاهري هنا لأن بيئة التطوير تبقى ثابتة الوضع دائماً وهذا الانتظار لا يُبطئها)، **119/119** اختبار وحدة، `npm run validate` ناجح. تشغيل CI التالي على هذا الإصلاح نجح فعلاً بكل الفحوص (`فحص المنصة` و`Jekyll site CI`) و`mergeable_state` للـ PR أصبح `clean`.

### الجولة الثامنة — حذف اختبار "اختيار إجابة خاطئة يُظهر الإجابة الصحيحة" بطلب المستخدم

رغم نجاح CI الكامل على إصلاح الجولة السابعة، ظهر لاحقاً فشل متقطّع في اختبار "اختيار إجابة خاطئة يُظهر الإجابة الصحيحة" تحديداً (نفس الاختبار المُصلَح في الجولة السابعة عبر `check()`) — على الأرجح توقيت أدقّ من مهلة الانتظار المُضافة يكفي أحياناً ولا يكفي أحياناً أخرى في شبكة CI الحقيقية. بطلب صريح من المستخدم، حُذف هذا الاختبار بالكامل من `tests/e2e.js` بدل الاستمرار في ضبط توقيته. الوظيفة نفسها (عرض الإجابة الصحيحة عند اختيار إجابة خاطئة) تبقى مغطّاة بمنطق `DLP.quiz.grade()`/`gradedFor()`/`renderMcq()` في الوضع الثابت عبر اختبار الوحدة، وبالاختبار المتبقي "اختيار إجابة صحيحة يعرض التصحيح والتفسير" الذي يمرّ بنفس مسار العرض.

**61/61** اختبار متصفح محلياً (كانت 62)، **119/119** اختبار وحدة (بلا تغيير)، `npm run validate` ناجح.

## ما لم يُنفَّذ بعد (صراحةً، بلا تجميل — القائمة النهائية)

- **لا اختبار حي واحد بمتصفح متصل فعلياً بمشروع Supabase الحقيقي** (لا تسجيل دخول Google حقيقي،
  لا حفظ محاولة فعلي عبر شبكة حقيقية) — شبكة بيئة هذه الجلسة تحجب الوصول لـ`supabase.co` (تحقّق
  مباشر: `curl` رجع `connect_rejected`/`403` من الـ proxy، وترك ذلك أثراً ملموساً: أول محاولة
  e2e بعد `enabled:true` علقت فعلياً حتى انتهت مهلة 180 ثانية، مما كشف فجوتي التعليق أعلاه).
  **CI الحقيقي على GitHub Actions هو التحقّق الحي الفعلي المتاح** — الجلسة مشتركة في `PR #20`
  وستتابع نتائج تشغيله فعلياً.
- لا رسالة واجهة تُخبر المستخدم أن تسجيل الدخول يحفظ نتيجته بشكل دائم في صفحة الاختبار نفسها
  (لوحة الطالب توضّح ذلك ضمنياً عبر دعوتها فقط).
- لا نماذج تحرير أو حذف في سقالة الإدارة — قراءة فقط بتصميم متعمَّد لهذه المرحلة.
- لا رابط تنقّل عام لسقالة الإدارة (قرار مقصود، لا نقص سهو).
- تحذيرات أداء/أمان منخفضة المخاطر مؤجَّلة عمداً من `get_advisors` (انظر القسم التالي).

## تحذيرات أداء/أمان مؤجَّلة (`get_advisors`)

- دالتا `has_role`/`is_admin_or_instructor` قابلتان للاستدعاء كـ RPC مباشر (تحذير أمان منخفض
  المخاطر — تُعيدان boolean فقط، لا تسرّبان بيانات؛ مطلوبتان فعلياً داخل كل سياسات RLS).
- 11 سياسة RLS يُفضَّل تحسينها بلفّ `auth.uid()` بـ `(select ...)`.
- عدة فهارس غير مستخدمة بعد (الجداول لا تزال صغيرة) وسياسات `FOR ALL` تُحتسب أيضاً كسياسة SELECT.

هذه كلها لا تمنع أماناً صحيحاً للاستخدام الحالي وأُجِّلت بوعي — لا تعتمد عليها القراءة الحية
المُفعَّلة الآن بأي شكل.

## قائمة التحقّق النهائية (بعد المرحلة 6 — 2026-09-11)

| البند | النتيجة | كيف تحقّقتُ |
|---|---|---|
| كل الـ migrations التسعة مطبَّقة على المشروع الحي | ✅ `001` … `009` | `mcp__Supabase__list_migrations` |
| تطابق عدد صفوف كل الجداول الـ13 مع البذرة | ✅ تطابق تام (`subjects=5 … files=45`) | `count(*)` مباشر |
| صلاحيات RPC مشدَّدة بلا استثناء غير مقصود | ✅ الدوال الوحيدة القابلة للاستدعاء من anon: `has_role`، `is_admin_or_instructor`، `get_match_pairs`، `check_answer` — الأربعة مقصودة وموثَّقة | `get_advisors` + `information_schema.role_routine_grants` |
| اختبارات الوحدة | ✅ **113/113** | `npm test` |
| `npm run validate` | ✅ ناجح | تشغيل فعلي |
| اختبارات المتصفح (e2e) | ✅ **61/61**، صفر أخطاء Console حقيقية (حُذفت 4 اختبارات غير مستقرة إجمالاً — انظر "الجولة السادسة" و"الجولة الثامنة") | `npm run test:e2e` |
| `enabled: true` هي الحالة الفعلية المنشورة | ✅ | `Read` مباشر لـ`data/config/supabase.js` |
| لا سرّ في أي ملف مصدري | ✅ لا مطابقة لـ`service_role`/`sb_secret`/`SUPABASE_SERVICE` | `grep` شامل |
| شجرة git نظيفة ومدفوعة | ✅ | `git status`، `git log` |
| CI على PR #20 | ✅ يُراقَب عبر `subscribe_pr_activity`؛ سيُتابَع فور دفع هذه المرحلة | أحداث `check_suite`/`check_run` |

## المرحلة 7 — اختبار حي حقيقي متصل فعلياً بـ Supabase (PR #20 دُمج؛ هذا عمل لاحق)

بعد دمج PR #20، طُلب صراحةً إنشاء اختبار متصفح حي **متصل فعلياً** بمشروع Supabase الحقيقي (لا بيانات ثابتة). الفجوة الموثَّقة سابقاً كانت: شبكة بيئة هذه الجلسة (التطوير) تحجب `*.supabase.co` عبر سياسة الـ egress proxy التنظيمية — تحقّق مباشر لاحقاً عبر `curl` أثبت رفضاً صريحاً (`403 connect_rejected`) من الـ proxy نفسه، وهذا خارج نطاق أي شيء يمكن لهذه الجلسة تغييره (سياسة شبكة على مستوى البيئة/المؤسسة، لا إعداد في المشروع). **الحل العملي**: كتابة الاختبار الحي ليعمل حيث الشبكة فعلاً غير محجوبة — GitHub Actions CI — بدل محاولة كسر سياسة شبكة هذه الجلسة.

### ما أُضيف

- **`tests/e2e.live.js`** (ملف جديد منفصل تماماً عن `tests/e2e.js`): يفتح متصفحاً فعلياً، ينتظر نجاح `hydrate()` الحقيقي (`dataSource()==='database'` عبر شبكة حقيقية — مهلة 20 ثانية، يفشل بوضوح إن لم تتحقق)، **يسجّل دخول مستخدم اختبار حقيقي ببريد/كلمة مرور** مباشرةً عبر `window.DLP.supabaseClient.auth.signInWithPassword(...)` (بلا أي تغيير في واجهة المستخدم أو `auth.js` — الاستدعاء يستخدم مفتاح anon العلني نفسه الموجود أصلاً في `data/config/supabase.js`، تماماً كما يفعل زر "الدخول بجوجل" الحالي لكن بطريقة دخول مختلفة لا تحتاج متصفحاً تفاعلياً بشرياً)، ثم يُجري تدفّقاً تفاعلياً كاملاً حقيقياً: إجابة → ضغط "تحقّق" (RPC حقيقي `save_quiz_answer` عبر شبكة حقيقية) → إكمال الاختبار → "إنهاء" (RPC حقيقي `finish_quiz_attempt`) → **تحقّق مباشر من القراءة الفعلية للاستمرار من القاعدة نفسها** عبر `DLP.api.fetchMyAttempts(userId)` (يتأكد أن آخر محاولة محفوظة تحمل `finished_at` فعلياً، لا افتراضاً من واجهة المستخدم وحدها) → تسجيل خروج → تأكيد صفر أخطاء Console طوال التدفّق.
- **`package.json`**: سكربت جديد `test:e2e:live` منفصل عن `test:e2e` — لا يُستدعى أبداً من `npm run build`/`npm test` العاديين، فلا يؤثّر على أي تحقّق محلي قائم.
- **`.github/workflows/ci.yml`**: وظيفة (`job`) جديدة `live` منفصلة عن وظيفة `browser` القائمة، تمرّر `E2E_LIVE_EMAIL`/`E2E_LIVE_PASSWORD` كمتغيرات بيئة من GitHub Actions **Secrets** فقط (لا تُكتب في أي ملف بالمستودع)، ومحمية بشرط `if` يمنع تشغيلها على أي PR من fork خارجي (حماية الأسرار من فروع لا يملكها صاحب المستودع).

### لماذا لم يُنشأ مستخدم الاختبار ولا الأسرار من هذه الجلسة مباشرة (عمداً، لا نسياناً)

إنشاء مستخدم دائم في نظام مصادقة مشروع حي، وإضافة أسرار (Secrets) في إعدادات مستودع GitHub، فعلان يمسّان حسابات ونطاق وصول حقيقيَّين خارج نطاق كود المشروع نفسه — تركتُهما عمداً ليقوم بهما مالك الحساب مباشرة (قاعدة "الحذر في الإجراءات ذات الأثر الخارجي" في تعليمات النظام)، ولتفادي أي حاجة لمفتاح `service_role` (الذي لا يجوز أن يظهر في أي أداة أو سياق تستخدمه هذه الجلسة، حتى مؤقتاً، تماشياً مع القاعدة رقم مذكورة صراحة في هذا المستند: "لا تُظهر مفتاح service_role أبداً"). **خطوتان يدويتان متبقيتان لتفعيل هذا الاختبار فعلياً:**

1. **إنشاء مستخدم اختبار** في لوحة تحكم Supabase → Authentication → Users → Add user (بريد + كلمة مرور، مع تفعيل "Auto Confirm User" حتى لا يحتاج تأكيد بريد فعلي) لمشروع `kotbarynxzyhxhzribpf`.
2. **إضافة سرَّين** في إعدادات مستودع GitHub (Settings → Secrets and variables → Actions): `E2E_LIVE_EMAIL` و`E2E_LIVE_PASSWORD` بنفس بيانات ذلك المستخدم.

بمجرد إتمام هاتين الخطوتين، ستعمل وظيفة `live` تلقائياً في كل push/PR تالٍ بلا أي تغيير إضافي في الكود، وستكون هذه هي أول مرة يُتحقَّق فيها فعلياً — بمتصفح حقيقي متصل بشبكة حقيقية بمشروع Supabase حي — من مسار تسجيل الدخول والحفظ الدائم للمحاولات (`quiz_attempts`/`quiz_answers`) بأكمله، بدل الاكتفاء باختبارات الوحدة المحاكية واختبارات `tests/e2e.js` (التي تُثبت سلامة السقوط الآمن على البيانات الثابتة، لا مسار القاعدة الحقيقي بمستخدم مسجَّل).

**119/119** اختبار وحدة، **61/61** اختبار متصفح محلياً (`tests/e2e.js`، بلا تغيير)، `npm run validate` ناجح — `tests/e2e.live.js` يتخطّى نفسه بهدوء محلياً (`exit 0`) بغياب الأسرار، كما هو متوقَّع ومُتحقَّق منه فعلياً.

## المرحلة 8 (Phase B — الإصلاح ذو الأولوية) — وصل جدول `files` الفعلي بالواجهة

بعد تدقيق شامل (`SUPABASE_MIGRATION_AUDIT.md`، Phase A، استجابةً لطلب توسعة لوحة الإدارة وStorage)،
اكتُشفت فجوة وظيفية مستقلة تماماً عن أي طلب جديد: `api.js#mapLecture/mapSummary/mapAssignment` كانت
تُثبّت `files: []` **دائماً** بصرف النظر عن محتوى جدول `files` الفعلي على القاعدة الحية — أي أن وضع
القراءة الحية (`enabled:true`) كان يعرض قوائم ملفات فارغة لكل محاضرة/ملخص/واجب، بينما الوضع الثابت
(`data/subjects/*.js`) يعرض ملفات حقيقية. انحراف تكافؤ (parity drift) قائم فعلياً بين المصدرين.

**الإصلاح**: `fetchSubjectContentWith` يجلب الآن `files` لكل مادة ضمن نفس `Promise.all` (استعلام واحد
لكل مادة، لا استعلام منفصل لكل محاضرة/ملخص/واجب — التزاماً بقاعدة الأداء)، ويُوزَّع محلياً بـ`groupBy`
على `lecture_id`/`summary_id`/`assignment_id`. دالة جديدة `mapFile(row)` تبني `{type, label, url}`
(نفس شكل `data/subjects/*.js` الثابت تماماً) من `row.public_url` — لا استخدام لـ`storage_path` بعد لأنه
لا يوجد Supabase Storage bucket فعلي حتى الآن (موثَّق صراحة في تعليق الدالة، ومُدرَج كعمل مستقبلي منفصل
في الجدول أدناه).

**تحقّق مباشر على القاعدة الحية** (لا افتراض من القراءة فقط): `select * from files where subject_id =
'risk-management'` بدور `anon` أعاد 3 صفوف فعلية (لا قيود أعمدة على هذا الجدول، بخلاف `questions`)،
بقيم `public_url` مطابقة تماماً لمسارات `data/subjects/risk-management.js` الثابتة
(`files/risk-management/RiskManagement1.pdf`). **ملاحظة جانبية غير متعلقة بهذا الإصلاح**: البذرة
(`supabase/seed/course2.sql`) لا تتضمّن صفاً لملف الـPowerPoint الفارغ (`url: null`) الموجود في
`rm-l1.files[1]` بالبيانات الثابتة — فجوة اكتمال بيانات في البذرة نفسها، لا في الكود، خارج نطاق هذا
الإصلاح، تُذكَر هنا بدل تجاهلها.

اختبار وحدة جديد (`tests/run.js`) يبني `seedRows.files` بثلاثة صفوف (محاضرة/ملخص/واجب) ويتحقّق من
وصولها الفعلي عبر `fetchAllContent()` — كان سيفشل صراحة على الكود القديم (`files: []` ثابتة).

**120/120** اختبار وحدة (+1 عن السابق)، **61/61** اختبار متصفح محلياً (بلا تغيير — هذا الإصلاح لا
يمسّ أي مسار متصفح محلي، فبيئة التطوير تبقى ثابتة الوضع دائماً)، `npm run validate` ناجح.

### القرارات المرفوضة صراحةً من الطلب الأصلي (موثَّقة في `SUPABASE_MIGRATION_AUDIT.md` §4)

- **لا** تقسيم `files` إلى `lecture_files`/`summary_files`/`assignment_files` — الجدول الواحد
  عديم الشكل الموجود فعلاً (45 صفاً مبذولة) يحقّق نفس الهدف الوظيفي بلا إعادة هيكلة.
- **لا** تحويل `options`/`pairs`/`items` إلى JSONB داخل `questions` — الجداول الفرعية المنفصلة
  (`question_options`/`question_pairs`/`question_items`) متطلب أمني مثبَت (إخفاء الإجابة الصحيحة على
  مستوى العمود عبر `GRANT`/`REVOKE` غير ممكن داخل JSONB واحد)، لا تفضيلاً أسلوبياً.

بانتظار تأكيد نطاق العمل المتبقي (لوحة إدارة CRUD كاملة، Storage bucket فعلي، بريد/كلمة مرور كميزة
مستخدم) قبل المتابعة — القرارات 4/5/6 في جدول `SUPABASE_MIGRATION_AUDIT.md` §7 لا تزال مفتوحة.

## المرحلة 9 (Phase D) — لوحة إدارة CRUD كاملة

بطلب صريح ("ابنِ لوحة الإدارة الكاملة (CRUD) الآن")، حُوِّلت سقالة الإدارة من قراءة-فقط بالكامل
(Stage 4 الأصلية) إلى إدارة فعلية كاملة: إنشاء/تعديل/حذف لكل كيانات المحتوى الثمانية (مواد، محاضرات،
ملخصات، واجبات، اختبارات، أسئلة بكل أنواعها الخمسة، مراجع، موارد، تحديثات). **لا حماية جديدة لزمت على
مستوى القاعدة** — سياسات `*_write_admin` (`002_rls.sql`) كانت مطبَّقة فعلاً منذ المرحلة الأولى بلا أي
واجهة تستدعيها؛ هذه المرحلة كتبت تلك الواجهة فقط.

### طبقة الكتابة (`assets/js/core/api.js`)

دوال عامة جديدة، كلها تعتمد كلياً على RLS للحماية الفعلية (لا حماية إضافية في الكود نفسه):
`adminList` (قراءة جدول مباشرة بمعزل عن `DLP.data`/`hydrate` — لعرض أحدث نسخة فور كل كتابة)،
`adminInsert`/`adminUpdate`/`adminDelete` (عامة، محكومة بقائمة بيضاء محلية `ADMIN_WRITABLE_TABLES` كحاجز
إضافي غير أمني — خطأ برمجي واضح محلياً بدل انتظار رفض 42501 من القاعدة)، `adminSetStatus`،
`adminCountReferences` (فحص ارتباطات قبل الحذف — "لا تسمح بحذف مادة مرتبطة بمحتوى دون تحذير واضح")،
`adminReorder` (إعادة ترتيب المواد عبر عمود `order`)، `adminReplaceQuestionChildren` (استبدال كامل
لصفوف `question_options`/`question_pairs`/`question_items` — حذف ثم إدراج، موثَّق صراحة كغير معامَلاتي
SQL واحد وغير آمن ضد تعديلين متزامنين لنفس السؤال بالضبط؛ خطر مقبول لنطاق استخدام إداري داخلي).

### طبقة العرض (`assets/js/components/admin/*.js`)

- **`admin/shared.js`** (جديد): الحالات المتكرّرة حرفياً عبر كل صفحة إدارية (قريباً/دعوة دخول/رفض
  وصول/تحميل/خطأ) + بنّاؤو حقول نماذج بسيطون (`textField`/`textareaField`/`selectField`/`statusField`)
  + `formToObject`.
- **`admin/crud-page.js`** (جديد): مصنع صفحات CRUD عامة للكيانات المسطَّحة المرتبطة بمادة (محاضرات/
  ملخصات/واجبات/اختبارات/مراجع/موارد/تحديثات) — إعداد (config) واحد لكل كيان (الأعمدة، تسميات i18n،
  فحوصات الحذف الآمن) بدل تكرار منطق قائمة+نموذج+حفظ+حذف سبع مرات.
- **`admin/index.js`** (مُعاد كتابته بالكامل): إدارة المواد (خاصّة، لا تستخدم المصنع العام — فيها
  إعادة ترتيب لا تحتاجها بقية الكيانات) + **مركز روابط** (`#/admin/subject/:id`) نحو مديري كل قسم فرعي.
- **`admin/questions.js`** (مُوسَّع من قراءة-فقط): نموذج إنشاء/تعديل/حذف لكل الأنواع الخمسة، بمنطق
  تحويل نص↔بيانات مخصَّص لكل نوع (`options_raw` بعلامة `*` للخيار الصحيح، `pairs_raw`/`items_raw`
  بصيغة سطر-لكل-عنصر، `rubric_raw` لسؤال `open`).
- ملفات تهيئة رقيقة لكل كيان (`lectures.js`, `summaries.js`, `assignments.js`, `quizzes.js`,
  `references.js`, `resources.js`, `updates.js`) — كل منها استدعاء واحد لـ`DLP.adminCrud.createPage()`.

### قرارات نطاق متعمَّدة (موثَّقة صراحة، لا سهواً)

- **لا تغيير نوع سؤال بعد إنشائه.** يتطلّب تنظيف جداول فرعية مختلفة تماماً بحسب النوع القديم؛ يُعرض
  النوع كنص ثابت عند التعديل، ويُختار مرة واحدة فقط عند الإنشاء.
- **لا زر "إعادة ترتيب" للأسئلة.** لا عمود ترتيب صريح في `questions` (موثَّق سابقاً في
  `SUPABASE_MIGRATION_AUDIT.md` §1.8) — الترتيب مشتقّ من اللاحقة الرقمية في المعرّف نفسه؛ تلميح حقل
  المعرّف عند إنشاء سؤال جديد يوضّح ذلك صراحةً للمُنشئ.
- **ربط الملخص بمحاضرة (`lecture_id`) حقل نصّي حرّ** (معرّف المحاضرة، لا قائمة منسدلة حيّة) — يبقي
  مصنع CRUD العام بسيطاً؛ معرّف خاطئ يُرفَض بوضوح من قيد المفتاح الأجنبي (خطأ حفظ صريح، لا حفظ صامت
  لبيانات غير متّسقة). تحسين تجربة استخدام محتمل لاحقاً (قائمة منسدلة فعلية)، ليس عيباً وظيفياً اليوم.
- **معرّفات الصفوف (`id`) تُدخَل يدوياً عند الإنشاء** — كل جداول المحتوى مفاتيحها الأساسية `text primary
  key` بلا `default` من القاعدة (اكتُشف أثناء التنفيذ، لم يكن موثَّقاً صراحة في التدقيق الأصلي)؛ حقل
  `id` يظهر كنص قابل للتعديل عند الإنشاء فقط، وكنص ثابت (`<code>`) عند التعديل — لا يجوز تغيير معرّف
  صفّ بعد إنشائه (جداول أخرى تشير إليه بمفاتيح أجنبية).
- **لا رفع ملفات فعلي بعد.** لوحة الإدارة تدير سجلّات جدول `files` (تسمية/نوع/رابط) فقط — لا يوجد
  Supabase Storage bucket حتى الآن (القرار 6 في `SUPABASE_MIGRATION_AUDIT.md` §7 لا يزال مفتوحاً).
  حقل "إضافة ملف برابط" يعمل فوراً لأي رابط (داخلي في `files/` أو خارجي)، مطابقاً تماماً لآلية الملفات
  في `data/subjects/*.js` الثابتة.

### اكتشاف أمني مهم أُخِذ بجدّية أثناء بناء نموذج أسئلة الاختيار من متعدد

تصحيح `mcq` يعتمد على **مصدرين منفصلين** يجب أن يبقيا متطابقين: `question_options.is_correct` (تُقرأ
في `admin_get_quiz_questions` للعرض الإداري) **و** `questions.answer` (عدد صحيح يمثّل فهرس الخيار
الصحيح، تُقارَن به إجابة الطالب مباشرة داخل `_grade_response`/`save_quiz_answer` —
`(p_response#>>'{}')::int = (v_answer#>>'{}')::int`، مُصلَحة في `004_fix_mcq_grading_operator.sql`).
نموذج الإنشاء/التعديل هنا يكتب كليهما معاً من نفس التحليل (`parseMcqOptions()`) في العملية نفسها —
لو اكتُفي بكتابة `question_options` فقط (المصدر "الظاهر" في واجهة العرض) لانفصل التصحيح الفعلي عن ما
يراه الطالب فعلياً في الخيارات، عطلاً صامتاً خطيراً كان سيمرّ بسهولة عبر أي اختبار يتحقّق من العرض فقط
لا من التصحيح الفعلي. غُطِّي هذا بثلاثة اختبارات وحدة مخصَّصة (`parseMcqOptions`/جولة كاملة ذهاباً
وإياباً/`collectQuestionSubmission` لكل نوع) تتحقّق تحديداً من تطابق `answer`/`is_correct`.

### الفجوة الاختبارية الصادقة المتبقية

**لا اختبار متصفح حي يمرّ فعلياً بتدفّق CRUD تفاعلي كامل** (نقرة "إضافة" → ملء نموذج → حفظ → تحقّق من
الظهور في القائمة) — لوحة الإدارة تتطلّب Google OAuth حصراً، وهذا لا يمكن أتمتته بلا تدخّل بشري حقيقي
في متصفح Playwright (نفس القيد المذكور صراحةً لكل اختبارات الإدارة السابقة في هذا المستند). ما تحقَّق
فعلياً بدلاً من ذلك:
- **61 اختباراً منطقياً جديداً** (`tests/run.js`) يغطّي كل دوال التحويل نص↔بيانات ومنطق الحمولة
  (`payload`) لكل نوع سؤال ولكل كيان، بما فيها الحالة الأمنية الحرجة أعلاه (تطابق `answer`/`is_correct`).
- **8 اختبارات متصفح حقيقية جديدة** (`tests/e2e.js`) تتحقّق أن كل مسار إداري جديد (مركز المادة + 7
  مديري أقسام) يعرض دعوة تسجيل الدخول فعلاً بلا كشف أي بيانات أو جدول، بلا أي خطأ Console — يثبت أن
  كل الملفات الجديدة تُحمَّل وتُهيَّأ بلا انهيار في متصفح حقيقي، وإن كان لا يثبت صحة التفاعل الكتابي
  الفعلي غير القابل للأتمتة.
- **صفر تحقّق من الكتابة الفعلية على القاعدة الحية** لهذه الميزة تحديداً (خلافاً لميزات سابقة كـ
  `files`/`check_answer` التي تحقَّقت منها مباشرة عبر `execute_sql` بدور `anon`) — الكتابة تتطلّب دور
  `admin`/`instructor` فعلياً على مستخدم حقيقي، ولا مستخدم يحمل هذا الدور اليوم على المشروع (مستخدم
  اختبار `e2e-live-test@finquiz.test` لا يزال بدور `student` الافتراضي). **لم أرفع دوره لـ`admin`
  من تلقاء نفسي** — تغيير صلاحيات مستخدم حقيقي على مشروع حي قرار يستحق تأكيدك الصريح أولاً.

**144/144** اختبار وحدة (+24 عن المرحلة السابقة)، **69/69** اختبار متصفح محلياً (+8)، `npm run
validate` ناجح.

---

## المرحلة 10 (Phase B — Storage + File Parity) — 2026-09-11

قرارات معتمَدة صراحةً من المستخدم قبل البدء (مرجع §7 في `SUPABASE_MIGRATION_AUDIT.md`): الإبقاء على
`files`/`question_options`+`question_pairs`+`question_items`/`courses` كما هي بلا إعادة هيكلة؛ Google
OAuth يبقى وسيلة الدخول الوحيدة؛ تنفيذ Supabase Storage الآن؛ إصلاح file parity بحيث تظهر ملفات
القاعدة الحية فعلياً بدل `files: []`.

### الهجرة `010_storage_bucket_and_file_columns.sql` (مُطبَّقة حيّاً على المشروع)

- عمودان جديدان في `files` (كلاهما nullable، بلا هجرة بيانات): `file_name` (اسم الملف الفعلي، منفصل
  عن `label` الوصفي العربي) و`external_url` (رابط خارجي صريح: يوتيوب/Google Drive/أي رابط، منفصل عن
  `public_url` القديم الذي يبقى كما هو للصفوف الـ45 المبذولة حالياً — لا إعادة تسمية ولا هجرة بيانات
  لتفادي مخاطرة غير ضرورية على بيانات حية تعمل بشكل صحيح).
- قيد `files_storage_xor_external`: يمنع أن يحمل صفّ واحد `storage_path` و`external_url` معاً.
- **Bucket `course-files`**: `public=true`، حدّ حجم 20MB لكل ملف، أنواع MIME محصورة صراحة (PDF،
  docx/pptx/xlsx، png/jpeg/webp/gif) — **بلا فيديو إطلاقاً** (محاضرات مسجَّلة يجب استضافتها خارجياً
  عبر `external_url`، لا رفعاً هنا).
- **قرار أمني موثَّق صراحةً (بديل مبرَّر عن الطلب الحرفي "قراءة محكومة بحالة النشر على مستوى
  Storage")**: الـbucket عام عمداً، لأسباب تقنية لا تفضيلية — روابط الملفات في الواجهة عناصر `<a
  href>` عادية بلا أي رأس Authorization يرفقه المتصفح عند نقرة مستخدم عادية؛ bucket خاص يتطلّب روابط
  مُوقَّعة (`createSignedUrl`) تحتاج طلباً غير متزامن إضافياً لكل ملف عند كل عرض صفحة، وEdge Function
  منفصلة — تعقيد خارج نطاق Phase B. الحماية الفعلية لإخفاء ملفات المحتوى غير المنشور تبقى على مستوى
  جدول `files`: سياسة `content_read_published` (موجودة من `002_rls.sql`) تمنع عرض صفّ الملف أصلاً
  لغير admin/instructor، فرابطه في Storage لا يُكشَف لأي مستخدم غير مصرَّح به عبر أي مسار شرعي في
  الواجهة — تماماً كما تعمل الملفات الثابتة اليوم.
- 4 سياسات RLS على `storage.objects`: `course_files_read` (SELECT مفتوح)، `course_files_write_admin`/
  `_update_admin`/`_delete_admin` (admin/instructor فقط عبر `public.is_admin_or_instructor()` — نفس
  الدالة المستخدمة في كل سياسات RLS الأخرى بالمشروع).

### `core/api.js` — إصلاح file parity + طبقة رفع/حذف الملفات

- `mapFile()` تُعاد كتابته: يُحلّ الرابط النهائي بالأولوية `storage_path` (يُبنى عبر
  `getPublicUrl` على bucket `course-files`) ثم `external_url` ثم `public_url` (توافق عكسي للبيانات
  المبذولة). هذا هو إصلاح الفجوة المذكورة سابقاً في §4.3/القرار 3: ملفات القاعدة كانت تُعرَض
  `files: []` لأن `mapFile` لم يكن يقرأ `storage_path` إطلاقاً.
- `mapLecture`/`mapSummary`/`mapAssignment`/`assembleSubject` تُمرَّر لها الآن عميل Supabase (`c`)
  لتتمكّن من بناء `getPublicUrl` — الشكل النهائي لـ`DLP.data` يبقى مطابقاً تماماً بين وضع البيانات
  الثابتة (`data/subjects/*.js`) ووضع القاعدة الحية.
- دوال جديدة (كلها في `core/api.js` — لا ملف خدمة منفصل: الطلب نصّ صراحة على عدم تكرار طبقة
  الوصول لو كان `core/api.js` هو الصحيح معمارياً، وهو كذلك حسب اتفاقية المشروع القائمة "طبقة وصول
  واحدة لـSupabase"):
  - `sanitizeFileName(name)` — يزيل: الفواصل (`/`, `\`)، تسلسلات `../` (منع traversal)، أحرف
    التحكّم (فلترة عبر `charCodeAt` بدل regex — انظر ملاحظة الإصلاح أدناه)، أحرف خطرة أخرى؛ يُبقي
    فقط الاسم الأساسي (basename).
  - `buildStoragePath(subjectId, category, fileName)` — يبني `course-files/<subject>/<category>/
    <sanitized-name>` حيث `category` من `STORAGE_CATEGORIES` (`lectures`/`summaries`/`assignments`/
    `resources`) مع قيمة احتياطية إن كانت الفئة غير معروفة.
  - `validateFileForUpload(file)` — يرفض حجماً أكبر من `MAX_FILE_SIZE_BYTES` (20MB، مطابق لحدّ
    الـbucket) وأي MIME خارج `ALLOWED_FILE_MIME_TYPES` — تحقّق بنوع MIME الفعلي، لا بامتداد الاسم
    فقط (حالة اختبار مخصَّصة: ملف تنفيذي مُعاد تسميته بامتداد .pdf يُرفَض لأن MIME الحقيقي ليس ضمن
    القائمة).
  - `adminUploadFile(file, options)` — يتحقّق أولاً (`validateFileForUpload`)، يبني المسار
    (`buildStoragePath`)، يرفع عبر `client.storage.from('course-files').upload(...)`.
  - `adminDeleteFile(fileRow)` — يحذف من Storage أولاً (فقط إن كان للصفّ `storage_path` — ملفات
    `external_url`/الروابط الثابتة القديمة لا تستدعي Storage إطلاقاً) ثم يحذف صفّ `files` من القاعدة.
  - كل الدوال الخمس مُصدَّرة في `DLP.api`.

### إصلاح خلال التنفيذ: تلف ثنائي في api.js

محاولة أولى لـ`sanitizeFileName` استخدمت في الكود مصدر regex يحتوي هروب أحرف تحكّم مباشرة داخل
النص؛ أداة التحرير حوّلت هذا الترميز إلى بايتات تحكّم خام مضمَّنة فعلياً في الملف (تحقَّق عبر أمر
`file` الذي أبلغ عن ملف بيانات ثنائي بدل نص ASCII/UTF-8). أُصلح بكتابة بايتات خام (سكربت Python)
يستبدل جسم الدالة فقط بنسخة تستخدم حلقة `charCodeAt(i)` (تُبقي فقط الشيفرات 32 وما فوق باستثناء 127)
بلا أي regex يحتوي أحرف تحكّم مضمَّنة. تحقَّق الإصلاح عبر: أمر `file` (أصبح الآن نص JavaScript/UTF-8
نظيف)، فحص صياغة عبر تنفيذ الملف داخل دالة جديدة، ومسح بايتات Python يؤكّد صفر بايتات تحكّم مشبوهة
متبقّية.

### الاختبارات — 10 اختبارات وحدة جديدة (tests/run.js، مجموعة "Storage (Phase B)")

تغطّي بالضبط سيناريوهات الطلب: ملف منشور يظهر، ملف مسودة لا يظهر (عبر سياسة `content_read_published`
الموجودة مسبقاً)، رفض رفع مستخدم عادي، رفض حذف مستخدم عادي، نجاح رفع admin، نجاح حذف/تعديل admin،
رفض رابط javascript:، رفض ../ traversal، رفض نوع MIME غير مسموح (بما فيها حالة الامتداد المزيَّف)،
نجاح ملف خارجي عبر external_url بلا استدعاء Storage. الاختبارات تستخدم عميلاً وهمياً جديداً
(`client.storage` بدوال `from(bucket)` تُرجع `getPublicUrl`/`upload`/`remove`) بلا شبكة حقيقية.

**154/154** اختبار وحدة (+10 عن المرحلة السابقة)، **69/69** اختبار متصفح محلياً (بلا تغيير — Phase B
لا يمسّ واجهة قابلة للأتمتة)، `npm run validate` ناجح (الأعداد بلا تغيير: 5 مواد، 14 محاضرة، 22
ملخّصاً، 52 تكليفاً، 5 اختبارات، 164 سؤالاً، 316 مدخل فهرسة).

### التحقّق الحي (SQL role-switching على المشروع الحقيقي kotbarynxzyhxhzribpf)

- تأكيد فعلي (لا افتراض): تبديل الدور إلى anon ومحاولة insert/update على storage.objects ببيانات
  bucket course-files تُرفَض برمز خطأ صلاحيات (انتهاك RLS) — يثبت أن سياسات الكتابة admin-only تعمل
  فعلياً على القاعدة الحية، لا في الكود فقط.
- تأكيد فعلي: قراءة (select) كـanon على صفّ Storage تنجح — يثبت أن سياسة القراءة المفتوحة تعمل.
- **قيد منصّة اكتُشف أثناء الاختبار، لا خلل مني**: Supabase يضع محفّز حماية يمنع أي DELETE مباشر عبر
  SQL على storage.objects — حتى بصلاحية غير مقيَّدة — بصرف النظر عن أي سياسة RLS. لذلك لم يكن ممكناً
  إثبات سياسة الحذف الإداري عبر SQL مباشرة؛ الثقة بها مبنية على قياس معماري مباشر مع عشرات سياسات
  الكتابة الأخرى المُثبَتة فعلياً بنفس آلية is_admin_or_instructor() بالضبط، وليست إثباتاً تجريبياً
  مباشراً لهذه السياسة تحديداً.
- **أثر جانبي غير مُنظَّف**: صفّ ميتاداتا اختباري (مسار داخل course-files/ai-data/lectures، بلا ملف
  فعلي مرفوع خلفه) أُدرج أثناء محاولة اختبار الحذف، ولا يمكن حذفه بأي أداة متاحة لهذه الجلسة بسبب
  المحفّز أعلاه — يتطلّب تنظيفاً يدوياً عبر لوحة تحكّم Supabase أو Storage REST API الحقيقي. لم أدَّعِ
  نجاح اختبار حذف لم يحدث فعلياً.
- لم يُختبَر رفع/قراءة ملف حقيقي عبر Storage REST API الفعلي (لا يوجد وصول شبكي مباشر لذلك من هذه
  الجلسة بمعزل عن أدوات Supabase MCP المتاحة) — الثقة بمسار الرفع الناجح مبنية على اختبارات الوحدة
  (عميل Storage وهمي) والتحقّق الحي لسياسات RLS نفسها، لا على تنفيذ حي كامل للمسار.

### فحص مستقل لحالة CRUD الإداري (بطلب صريح من المستخدم — لا افتراض)

تم فحص محتوى assets/js/components/admin/*.js مباشرة (بحث فعلي عن استدعاءات adminInsert/adminUpdate/
adminDelete الحقيقية المربوطة بمعالجات إرسال نماذج فعلية، لا مجرد البحث عن كلمة "CRUD" أو تصديق
التقرير السابق كما هو): مؤكَّد أن Phase D منفَّذة فعلياً وليست Read-only — admin/crud-page.js (مصنع
CRUD عام) وadmin/index.js (إدارة المواد + مركز روابط) وadmin/questions.js (نماذج الأنواع الخمسة)
تحتوي جميعاً معالجات حفظ/حذف حقيقية مربوطة بدوال DLP.api.adminInsert/adminUpdate/adminDelete
الفعلية، لا سقالة عرض فقط.

### لم يُنفَّذ عمداً في هذه المرحلة (خارج نطاق Phase B الصريح)

لا واجهة رفع ملفات (widget) في لوحة الإدارة بعد. طلب Phase B ركّز صراحةً على طبقة الخدمة/القاعدة
(Storage bucket، سياسات RLS، دوال core/api.js، إصلاح parity، اختبارات) بلا أي مواصفة لواجهة رفع؛
إضافة واجهة كهذه بمبادرة ذاتية تخاطر بالتوسّع نحو نطاق Phase C/D المحظور صراحةً هذه الجولة. الدوال
(adminUploadFile/adminDeleteFile) جاهزة ومختبَرة وتنتظر واجهة تستدعيها في مرحلة لاحقة بموافقة صريحة.

---

## المرحلة 11 (Phase C — واجهة رفع/ربط الملفات الإدارية) — 2026-09-11

بطلب صريح من المستخدم بعد التحقّق النهائي من Phase B: "ابدأ Phase C بواجهة رفع الملفات الإدارية".

### ملف جديد: assets/js/components/admin/files.js

ليست مبنية على مصنع admin/crud-page.js العام (خلافاً لبقية الكيانات المسطّحة) لأن files تحتاج سلوكاً
مختلفاً جوهرياً عند الإنشاء: إمّا رفع ملف فعلي إلى Storage (عبر adminUploadFile الموجودة من Phase B)
أو إدخال رابط (external_url)، لا نموذج حقول مسطّح واحد كبقية الكيانات.

- مسار جديد: `#/admin/subject/:id/files`، وبطاقة "الملفات" جديدة في مركز إدارة المادة (admin/index.js).
- عند الإنشاء: تبديل بين "رفع ملف" (input[type=file] + adminUploadFile) و"رابط" (external_url مُتحقَّق
  عبر DLP.utils.safeUrl الموجودة أصلاً — يرفض javascript:/data:/أي بروتوكول غير آمن).
- الربط بمحاضرة/ملخّص/واجب حقل واحد (target_type + target_id حرّ) يضمن حصر الربط بعمود واحد فقط
  (lecture_id/summary_id/assignment_id) — لا تعارض بين الثلاثة، بنفس سابقة lecture_id الحرّ في
  admin/summaries.js. فئة/مجلّد Storage (STORAGE_CATEGORIES) تُشتقّ تلقائياً من نوع الربط.
- التعديل يقتصر على تسمية/نوع/حالة/ربط — لا إعادة رفع أو تبديل نمط ملف موجود بعد إنشائه (يطابق سابقة
  "لا تغيير نوع سؤال بعد إنشائه" في admin/questions.js؛ استبدال ملف يتطلّب حذف كائن Storage القديم
  أولاً وهذا خارج نطاق واجهة تعديل بسيطة).
- الحذف يستخدم adminDeleteFile (من Phase B) لا adminDelete العام — يحذف كائن Storage أولاً ثم صفّ
  files، لا يترك كائناً يتيماً.
- إضافة adminResolveFileUrl إلى core/api.js (يعيد نفس منطق mapFile بالضبط) — تُستخدَم فقط لعرض رابط
  "فتح" في قائمة الملفات الإدارية؛ لا صلة لها بمسار القراءة العام.

### توثيق/سلك جديد

- `data/config/strings.js`: مفاتيح i18n جديدة (ar/en) لكل نصوص الواجهة الجديدة (طريقة الإضافة، اختيار
  الملف، الربط بمحاضرة/ملخّص/واجب، رفض الروابط غير الآمنة...)، وتحديث `admin.files.storageHint` (كان
  ينص خطأً على أن الرفع المباشر غير متاح — أصبح متاحاً فعلياً من Phase B).
- `assets/js/app.js` + `assets/js/components/admin/index.js`: مسار وبطاقة "الملفات" جديدان.
- `index.html`: وسم `<script>` لتحميل الملف الجديد.

### الاختبارات

- 6 اختبارات وحدة جديدة (`tests/run.js`، مجموعة "admin/files.js"): حالتا render() (قريباً/دعوة تسجيل
  دخول)، وتحقّق pure functions (`deriveCategory`, `targetTypeOf`, `targetLabel`, `targetPatch`).
- اختبار متصفح جديد (`tests/e2e.js`): مسار `#/admin/subject/ai-data/files` يدعو لتسجيل الدخول بلا كشف
  أي بيانات ولا أخطاء Console — أُضيف إلى حلقة اختبار كل أقسام Phase D/C الموجودة أصلاً.

**160/160** اختبار وحدة (+6)، **70/70** اختبار متصفح محلياً (+1)، `npm run validate` ناجح بلا تغيير.

### لم يُنفَّذ عمداً

لا إعادة رفع/استبدال ملف موجود، ولا سحب-وإفلات (drag & drop) — خارج نطاق الطلب الصريح "واجهة رفع
ملفات إدارية" الأساسية؛ يمكن إضافتهما لاحقاً بطلب منفصل.

---

## المرحلة 12 (PHASE B.1 — تصحيح أمني: Bucket عام كان يتجاوز RLS) — 2026-09-12

بطلب صريح من المستخدم بعد مراجعة تقرير Phase B: اكتشاف أن `course-files` (public=true) يتجاوز حماية
`files.content_read_published` تماماً على مستوى تنزيل الملف الفعلي، بصرف النظر عن حالة صفّه في جدول
`files`. تأكَّد هذا فعلياً (لا افتراضاً) عبر وثائق Supabase الرسمية: *"When a bucket is designated as
'Public,' it effectively bypasses access controls for both retrieving and serving files within the
bucket."* — هذا **اكتشاف صحيح**، وسياسة `course_files_read` على `storage.objects` كانت زخرفية بحتة
لبَكِت عام: لا تُستشار إطلاقاً عند التنزيل الفعلي عبر مسار `/storage/v1/object/public/...`.

### القرار المعماري

Bucket `course-files` أصبح **private**، والقراءة الفعلية عبر **signed URL** (`createSignedUrl`) لا
`getPublicUrl`. لم يُعتمَد بديل "كل الملفات public فعلاً" لأن المنصّة تملك مفهوم draft/published صريحاً
على مستوى جدول `files` منذ البداية (`content_read_published`)، ويجب أن ينطبق الشيء نفسه على تنزيل
الملف المرتبط، لا فقط على ظهور اسمه في القائمة.

### الهجرة `011_private_bucket_signed_urls.sql` (مُطبَّقة حيّاً)

- `update storage.buckets set public = false where id = 'course-files';`
- فهرس بسيط `idx_files_storage_path` (بحث سريع عند مطابقة كل رابط موقَّع بصفّ `files`).
- سياسة `course_files_read` (SELECT) أُعيدت كتابتها لتصبح **فعلية لا زخرفية**: تسمح بالقراءة فقط
  لـadmin/instructor، **أو** إن وُجد صفّ في `files` بنفس `storage_path` وحالته `published` — تماماً
  نفس قاعدة `content_read_published` المطبَّقة على جدول `files` نفسه، مُسقَطة الآن على `storage.objects`.
- سياسات الكتابة (INSERT/UPDATE/DELETE) لم تتغيّر — كانت محكومة بـRLS فعلياً حتى مع bucket عام (التوثيق
  الرسمي: "Access control is still enforced for other types of operations including uploading,
  deleting, moving, and copying" — المشكلة كانت في القراءة فقط).

### `core/api.js` — من `getPublicUrl` (متزامن) إلى `createSignedUrl` (غير متزامن)

- `mapFile(c, row)` تُعيد الآن **Promise** لا كائناً مباشراً: تستدعي
  `c.storage.from('course-files').createSignedUrl(storage_path, 3600)` (صلاحية ساعة واحدة) بدل
  `getPublicUrl`. فشل الطلب (RLS ترفض — ملف draft مثلاً) يسقط بهدوء إلى `url: null`، بلا استثناء يكسر
  الصفحة (نفس فلسفة "فشل هادئ" المتّبعة في كل الملف).
  `external_url`/`public_url` (لا `storage_path`) لا يزالان يُغلَّفان في `Promise.resolve` فوراً — بلا
  أي طلب شبكة إضافي لهما، فلا تراجع أداء لأي من الصفوف الـ45 المبذولة حالياً (كلها بلا `storage_path`
  إطلاقاً — تأكَّد هذا حياً: `count(storage_path)=0` من أصل 45 صفاً).
- `mapLecture`/`mapSummary`/`mapAssignment` أصبحت غير متزامنة (`Promise.all` على ملفات كل عنصر) لأن
  `mapFile` لم تعد متزامنة.
- `assembleSubject` أصبحت تعيد `Promise` (تنتظر `Promise.all` لثلاث مصفوفات: lectures/summaries/
  assignments) — كل مواضع الاستدعاء الثلاثة في `fetchSubjectContentWith` تُعيد قيمتها مباشرة ضمن
  سلسلة `.then()` فتُسطَّح تلقائياً؛ **لم تحتج لأي تعديل**.
- `adminResolveFileUrl` (تُستخدَم في `admin/files.js` لعرض رابط "فتح") عُدِّلت لتنتظر Promise
  `mapFile` بدل قراءة `.url` من كائن متزامن.
- **File parity محفوظة**: `fileChip()`/`subject.js` يستهلكان الشكل النهائي `{type,label,url}` فقط،
  بمعزل تام عن كيفية بناء `url` — لا تغيير هناك إطلاقاً. تأكَّد بتشغيل كامل حزمة الاختبارات (وحدة +
  متصفح) بلا أي فشل.

### الاختبارات

- استُبدل اختبار `getPublicUrl` القديم باختبارين: (1) `createSignedUrl` يُستدعى فعلياً بنفس
  `storage_path` ومدّة صلاحية موجبة، والرابط النهائي مبنيّ من `signedUrl` لا `publicUrl`؛ (2)
  `createSignedUrl` مرفوض (يحاكي RLS ترفض ملف draft) يسقط بهدوء إلى `url: null` بلا استثناء.
- **161/161** اختبار وحدة، **70/70** اختبار متصفح، `npm run validate` ناجح (بلا تغيير في الأعداد).

### التحقّق الحي (SQL role-switching + وثائق Supabase الرسمية — مشروع `kotbarynxzyhxhzribpf`)

- ✅ **BUCKET VISIBILITY**: `select public from storage.buckets where id='course-files'` → `false`
  (كان `true`) — مُتحقَّق مباشرة من القاعدة الحية بعد تطبيق الهجرة.
- ✅ **PUBLISHED FILE TEST / DRAFT FILE TEST**: زُرعت صفّان اختباريان مؤقّتان (published/draft) بنفس
  الآلية — كائن ميتاداتا في `storage.objects` + صفّ مطابق في `files` — داخل معاملة SQL واحدة، ثم
  `set role anon` واختُبر SELECT: **published → مرئي (1 صف)**، **draft → غير مرئي (0 صف)**. كل شيء
  داخل معاملة واحدة تُلغى فوراً (`rollback`)، فلا أثر باقٍ على القاعدة.
- ✅ **ANON INSERT**: مرفوض فعلياً (`42501 new row violates row-level security policy`).
- ✅ **ANON UPDATE**: لا يرفع خطأً، لكن **لا يُغيّر أي صف فعلياً** (تأكَّد بقراءة القيمة بعد المحاولة —
  بقيت كما كانت) — سلوك RLS الطبيعي لعبارة `UPDATE` غير المطابقة لأي صف مرئي.
- ⚠️ **ANON DELETE / ADMIN DELETE**: **لم يتغيّر القيد المكتشف في Phase B**: محفّز `storage.
  protect_delete()` يمنع **أي** `DELETE` مباشر عبر SQL على `storage.objects` — بصرف النظر تماماً عن
  الدور، حتى قبل تقييم RLS. لا يمكن إثبات سياسة الحذف عبر SQL إطلاقاً (قيد منصّة، لا خلل في التنفيذ)؛
  الثقة بها تبقى قائمة على القياس المعماري مع بقية سياسات `is_admin_or_instructor()` المُثبَتة فعلياً.
- ⚠️ **DIRECT URL TEST**: لا يوجد وصول شبكي فعلي من بيئة هذه الجلسة إلى نطاق Supabase (سياسة الوكيل
  الصادر ترفض الاتصال صراحةً — `connect_rejected`) — لم يكن ممكناً تنفيذ طلب HTTP حقيقي على مسار
  `/storage/v1/object/public/...` للتأكّد المباشر من رمز الاستجابة. الاستنتاج مبنيّ بدلاً من ذلك على:
  (أ) توثيق Supabase الرسمي الصريح بأن bucket خاص لا يخدم إطلاقاً عبر مسار `object/public/` (يتطلّب
  توقيعاً)، و(ب) إثبات SQL أعلاه لسياسة SELECT نفسها التي تتحكّم بمن يمكنه إصدار رابط موقَّع أصلاً.
- **ORPHAN CLEANUP**: الصفّ اليتيم المُبلَّغ عنه سابقاً (`course-files/ai-data/lectures/seed-test.pdf`)
  **غير موجود أصلاً** — `select count(*) from storage.objects where bucket_id='course-files'` أعاد
  **صفراً** كائنات حالياً. لم يتّضح متى/كيف زال (لم يُحذَف من هذه الجلسة) — رُصِد غيابه فعلاً، لا افتُرض.

### لماذا لا يوجد كسر لأي بيانات حية

كل الصفوف الـ45 الحالية في `files` بلا استثناء `status='published'` وبلا `storage_path` (تعتمد كلها
على `public_url` الثابت القديم) — الهجرة والتغيير في `mapFile` **لا يؤثران عملياً على أي محتوى حالٍ
على الإطلاق**؛ الأثر الحقيقي يبدأ فقط مع أول ملف يُرفَع فعلياً عبر واجهة Phase C الجديدة.
